# Queue triage (rolling)

Single living triage doc for `.work-queue.json`. Supersedes the dated
`QUEUE_TRIAGE-YYYY-MM-DD.md` snapshots and `QUEUE_PIPELINE_DIAGNOSIS-*.md`,
which were folded in here and removed; their point-in-time history remains in
git. The nightly queue-triage task overwrites the "Current state" section below
on each run, so this file always reflects the latest pass rather than spawning a
new dated file.

Last pass: 2026-07-24.
## Current state

Seventy-four non-auto-safe items are aging past 7 days (added on or before 2026-07-16), up from 56 last pass — the 2026-07-16 bar-raise/audit wave (12 Gnosis + Budget items, plus the Cattery P0) crossed the window all at once. The worst offenders by age are still the four **44-day** items from 2026-06-10 — `yac-store-launch`, `yac-subscriptions-enable-ai`, `hbh-asset-production-pass`, `br-asset-production-pass` — all genuine milestone gates, correctly parked. The largest backlog is **Skylight at 16**, then **Budget and Gnosis tied at 10**, then Cattery 9 and Ring 8.

**The headline is not age, it is a P0 that has been open eight days while nine releases shipped past it.** `bar-raise-2026-07-16-gnosis-secret-answers-ungated-api` was re-verified at HEAD `ca3e13f` this pass and is fully open: `web/worker.js:317` serves `/api/answers` and `:320` serves `/api/answers/export` with no gate check on either, returning the whole store to an anonymous caller. Gnosis shipped v0.3.250 → v0.3.258 in the meantime, all globe and lore work. Its sibling `release-gate-check-page-only` explains why nothing caught it: the deploy check asserts 401 on a *page*, and the page gate is not the data gate. The second P0, `canonical-audit-2026-07-16-cattery-orders-cascade-prod-unapplied`, was verified against live prod — `orders_cattery_id_fkey` still reports `confdeltype='c'` — so a breeder deleting their account still cascades away the escrow audit trail.

**Shape otherwise unchanged: findings age because the unattended no-publish drain can't ship judgment-bound work.** Everything re-verified this pass is still live. Ring's tree is dirty with the same 6 files as last pass and its seed PII (`public/data/seed.json`: "Pamela Ware", `pam@topspotsfarm.com`) still ships. Scheduler's tree is identical to last pass (6 modified + the 2 untracked). The HBH GDD still reads "10 registered classes" at `GDD.html:4264` against a measured 11. Budget's tree is newly dirty but harmlessly so — three comment/doc-path corrections in `backup.ts`, `db.ts`, `DESIGN.md`, not partial work on any queued fix.

**Two items got measurably worse, not better:** `bar-raise-2026-07-16-gnosis-buildmjs-seven-esc-defs` is filed against seven `esc` definitions in a 5,332-line file; `web/build.mjs` is now **6,345 lines with nine** `esc` definitions (100, 1117, 1367, 2087, 2590, 3599, 3871, 4017, 4097). And `bar-raise-2026-07-16-yab-finance-product-backup-cadence-02` predicted convergence to zero backups — both `X:\YesAndBudget\data\backups\` and `X:\.budget\backups\` hold **0 `.db` files** right now.

**One item is quietly two-thirds done:** `canonical-audit-2026-07-02-cattery-missing-migrations`. Files for `reviews_and_ratings` (0009) and `waitlists` (0010) now exist on disk, but untracked; the third, `admin_set_flags_self_lockout_guard` (ledger version 20260630183939), still has no file and is recoverable verbatim (1 statement in `schema_migrations.statements`). The `0006` divergence is confirmed in the other direction: `0006_photos_storage_bucket.sql` is in the repo but absent from the prod ledger.

**Standing concern, third pass running:** the five `br-bp-*` items have been `deferred` since 2026-06-16 (38 days) while Navy's art kept landing. The "stand up Git LFS BEFORE the real art set" window is closed. This batch needs a scheduled BR-best-practices session or a formal locked decision; perpetual defer is the wrong resting state.

### Hordes
Six items — two milestone gates, four drop-in.

- **hbh-asset-production-pass** (P1, added 2026-06-10) — Defer to the art-production milestone. Large Midjourney-batch work gated on Nick's personalization standard and the GDD/ASSETS.md gap inventory; not a one-session task.
- **hbh-multimesh-crowd-migration-2026-06-28** (P1, added 2026-06-28) — Do this session. `crowd3d.gd` is a complete-but-unwired MultiMeshInstance3D renderer; wire `world_3d.gd` EnemyField3D → Crowd3D to lift the 34fps@2000 / 22fps@3000 baseline. Self-contained.
- **hbh-velocity-off-critical-path-mission-layer-2026-07-10** (P1, added 2026-07-10) — Defer to a mission-layer milestone decision. Real strategic finding (daily ships are all art wiring while `source/data/mission.gd` doesn't exist and M3 First Playable stalls), but re-porting the mission data layer is a Nick-sized call; att2 blocked.
- **hbh-observability-frame-time-logger-2026-07-11** (P2, added 2026-07-11) — Do this session. Default-off, `debug_flags`-gated frame-time sampler reusing the fps/alive_count pair `_refresh_hud` already computes at world_3d.gd:522; additive, no per-frame cost.
- **hbh-gdd-world3d-classname-count-2026-07-11** (P3, added 2026-07-11) — Do this session. Re-verified again: GDD.html:4264 still reads "10 registered classes" vs a measured 11 `class_name` decls in source/world3d. One-word doc edit that has now survived two triage passes.
- **hbh-autopause-horde-day-signal-2026-07-14** (P1, added 2026-07-14) — Do this session. Read-only-verified dead subsystem: `auto_pause.gd` connect_* have zero callers, `wave_director.gd` never emits the horde-day signal, and the CP position is typed Vector2 in a 3D world. Scope already widened to the whole subsystem, with "hide the dead toggles and delete" recorded as an acceptable alternative — either resolution is one session.

### Rising
Seven items — one post-M1 gate, the five-item June best-practices batch, one mechanical doc reconcile.

- **br-asset-production-pass** (P2, added 2026-06-10) — Defer to post-M1. Correctly parked behind the M1 survival loop (the 40% blocking gate).
- **br-bp-terrain-tilemaplayer-2026-06-16** (P1, added 2026-06-16) — Defer to a scheduled BR best-practices session. Replace the immediate-mode ground render with a TileMapLayer+TileSet; real perf+correctness fix, batched with its siblings.
- **br-bp-polling-to-event-2026-06-16** (P1, added 2026-06-16) — Defer to the BR best-practices session. Convert ~54 per-frame group-poll `_process` loops to event/tick; prime lag suspect, same batch.
- **br-bp-stats-to-resources-2026-06-16** (P2, added 2026-06-16) — Defer to the BR best-practices session. Move enemy/building stat tables to `.tres` Resources and collapse the hand-mirrored scale constants; same batch.
- **br-bp-main-world-gui-scene-2026-06-16** (P3, added 2026-06-16) — Defer to the BR best-practices session. Author a Main → World + GUI baseline scene, moving static assembly out of gameplay.gd; same batch.
- **br-bp-git-lfs-2026-06-16** (P3, added 2026-06-16) — Do this session (Nick must greenlight the git ops). Precondition expired weeks ago — Navy's art has been landing since v0.59.35-36, so this should jump its own batch rather than wait for the session that keeps not happening.
- **canonical-audit-2026-07-11-rising-assets-tab-artdrop-stale** (P2, added 2026-07-11) — Do this session. Pure data-entry: bump the GDD Assets-tab `data-asset-default` values (14 building bodies, 5 units, 5 enemies, wall pair, Plains) to match the ART_INTEGRATION_NOTES.md phase checklist.

### Chains
Three items — two 44-day launch gates plus one security finding.

- **yac-store-launch** (P1, added 2026-06-10) — Defer to the Chains launch milestone. PWA store listings / TWA for Play Store; genuine launch work needing Nick, one of two 92%-to-done gates.
- **yac-subscriptions-enable-ai** (P1, added 2026-06-10) — Defer to the Chains launch milestone. Subscriptions billing build that then flips `CHAINS_VISIBLE` to enable the AI caddy; the second done gate.
- **chains-br-auth-01-2026-07-04** (P1, added 2026-07-04) — Do this session. HIGH: the Worker trusts client-supplied `body.userId` with no Supabase JWT verify (lib.js:1717-1734), so any known UUID spoofs Pro. Urgency is bounded (the paid AI caddy is still gated off), but this must land before subscriptions flip live — it is a prerequisite of `yac-subscriptions-enable-ai`, not a parallel item.

### Scheduler
Two items, both the same unlanded v0.7.x tree.

- **scheduler-auditlog-composite-index-2026-07-10** (P3, added 2026-07-10) — Do this session. Re-verified: `migrations/0011_audit_log_composite_index.sql` still on disk untracked, DESIGN.md aligned; commit it with the v0.7.x pass.
- **working-tree-2026-07-11-scheduler-stale-tree** (P2, added 2026-07-11) — Do this session. Byte-for-byte identical to last pass: 6 modified (index.test.ts, lib/users.ts, open-shifts.ts, schedule.ts, users route, DESIGN.md) + 2 untracked (the 0011 migration, sync-audit-dashboard.ps1). No churn in 13 days means nobody is mid-edit — one attended commit lands the whole quality pass safely.

### Apothecary
No qualifying items.

### Budget
Ten items — the two carried P1s plus the whole 2026-07-16 bar-raise wave. Every technical claim in the new wave was re-verified against the source this pass and all five hold.

- **bar-raise-2026-07-04-yab-bulk-delete-no-backup** (P1, added 2026-07-04) — Do this session. `POST /api/transactions/bulk-delete` (server.ts:534-544) is the only destructive route skipping `createBackup`; add `createBackup("pre-bulk-delete")` at the handler top to match every other mutating path. Small and clear — and now compounded by backup-cadence-02 below, since there are no backups to fall back on either.
- **bar-raise-2026-07-10-yab-plaid-takeover-window** (P1, added 2026-07-10) — Defer to Plaid Phase 2. The 90-day takeover window is hard-coded with an in-code TODO to ask Plaid for `earliest_available_date`; two failed attempts confirm the real fix needs the backfill integration, not a constant tweak.
- **bar-raise-2026-07-16-yab-data-integrity-02** (P1, added 2026-07-16) — Do this session, first in Budget. Verified: `sync.ts:102` uses a bare `ON CONFLICT(plaid_transaction_id)` while `db.ts:293` creates that index PARTIAL (`WHERE plaid_transaction_id IS NOT NULL`), which fails at prepare time. The whole Plaid sync path is dead on arrival and the error is swallowed into `status='error'`, so it replays and fails forever. Highest-value Budget item; the fix is adding the index predicate to the conflict target.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-04** (P1, added 2026-07-16) — Do this session, paired with data-integrity-02. It is explicitly the second half of the same bug: once the conflict target is fixed, the pending→posted transition collides on the `db.ts:120` natural key. Fixing di-02 alone converts a permanent failure into a rollback, so shipping them separately is worse than shipping neither.
- **bar-raise-2026-07-16-yab-finance-product-backup-cadence-02** (P1, added 2026-07-16) — Do this session. Verified live: both `X:\YesAndBudget\data\backups\` and `X:\.budget\backups\` contain **zero** `.db` files while the DB carries a current mtime. The predicted steady state is the actual state. Add a keep-newest-3 floor to `pruneOldBackups` plus a day-keyed boot snapshot.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-02** (P1, added 2026-07-16) — Do this session. Verified: `UNIQUE(account_id, posted_date, amount, raw_description)` at db.ts:120 has no occurrence ordinal, and the CSV path counts the collision as a duplicate and drops it — silent data loss on two genuine same-day same-amount charges, contradicting the project's own flag-don't-delete policy.
- **bar-raise-2026-07-16-yab-finance-product-threat-model-01** (P1, added 2026-07-16) — Do this session. Verified: `cors()` is mounted on `/*` at server.ts:90-100 with `https://yesandeverything.com` in the origin list, so the public site's origin can read `/api/transactions` and `/api/accounts` cross-origin with no auth to fail. Scope the broad allow-list to the probe route.
- **bar-raise-2026-07-16-yab-finance-product-categorization-tail-01** (P1, added 2026-07-16) — Do this session. Verified: `merchant.ts:14` strips `/\*+\d+/g`, which needs digits immediately after the asterisk, so `AMZN Mktp US*2X4RT8901` keeps its order token; with save-as-rule on by default this writes unmatchable rules forever. Chronic (7 runs open).
- **bar-raise-2026-07-16-yab-dependency-02** (P1, added 2026-07-16) — Do this session. Two halves, both small: Nick flips Dependabot alerts + security updates on in repo settings (one GitHub toggle), then the three docs that claim coverage that never existed (`dependabot.yml:3`, `SECURITY.md:26`, `SECURITY_POLICY.md:68`) get corrected. Neither half is worth deferring.
- **bar-raise-2026-07-16-cross-budget-openfindings-backfill** (P2, added 2026-07-16) — Defer to an attended queue-shape session with Kane. Parts (1) and (2) shipped and the leak self-heals going forward; part (3) would enqueue 63 unreviewed historical findings into a queue already at 74 aging items, burying the two P0s. That is a deliberate judgment call, correctly held.

### Everything
Three items — a self-resolved-but-unpublished skill table plus the same Discord-webhook decision split across two audits.

- **handler-audit-skill-table-update-2026-06-14** (P3, added 2026-06-14) — Defer to a Nick skill-republish (Settings → Capabilities). Re-verified: the staged source at `_skill-review/personal-skills-src/skills/handler-audit/SKILL.md` lists six handlers with correct post-rename paths, while the *installed* skill still describes four (HBH, YaC, Scheduler, YaE). The Claude-actionable half is finished; only the publish remains. At 40 days this is the oldest item in the portfolio that is genuinely one click from done.
- **handler-audit-2026-07-03-portfolio-discord-webhook-reconcile** (P2, added 2026-07-03) — Defer to a provision-vs-trim decision, and treat as the surviving item of the pair. Re-measured on disk: webhook files exist for Gnosis (6), Ring (4), Scheduler (3), Chains (1), Lexi (1), and the full sets for BR/HBH/Apothecary/Budget — but **zero** for Cattery, Agents, Skylight and Counselor. Cattery is the only real gap (Agents and Skylight are covered by the standing no-Discord rule), which narrows the decision considerably.
- **handler-audit-2026-07-10-webhook-boilerplate** (P2, added 2026-07-10) — Defer to the same decision and consolidate into the item above. Same underlying boilerplate mismatch, doc-accuracy only. These two should collapse into one; carrying both inflates the Everything backlog without adding information.

### Agents
No qualifying items.

### Ring
Eight items — six 07-07 bar-raise findings, one stale tree, one doc mismatch. Tree confirmed dirty (same 6 files as last pass); verify no live parallel session before any commit.

- **bar-raise-2026-07-07-ring-worker-observability** (P1, added 2026-07-07) — Do this session. Bare catch blocks swallow errors while `observability.enabled` is true but nothing is instrumented; add structured logging on the cron/rate-limit/upstream failure branches. Additive.
- **bar-raise-2026-07-07-ring-community-ratings-backup** (P1, added 2026-07-07) — Do this session, paired with dualwrite-atomicity. Judge ratings live in KV only (`j:<judge>`) with no replica; add a nightly KV→Postgres mirror reusing the `user_state` dual-write pattern.
- **bar-raise-2026-07-07-ring-dualwrite-atomicity** (P1, added 2026-07-07) — Defer to a Ring durability pass. KV+PG dual-write has no atomicity; needs a repair-on-read / reconcile design (att2 blocked). Batch with community-ratings-backup into one durability session.
- **bar-raise-2026-07-07-ring-worker-freetier-budget** (P1, added 2026-07-07) — Defer to a scale/cost pass. Explicitly anticipatory ("as coverage + users grow"); adaptive cron budgeting is a design task and Ring is nowhere near the limit.
- **bar-raise-2026-07-07-ring-gdpr-data-endpoints** (P1, added 2026-07-07) — Do this session. A paid product storing emails, cat names and judge notes has no export or deletion path; add gated `DELETE /api/user/data` (cascade) + `GET /api/user/export` + a retention/privacy page.
- **bar-raise-2026-07-07-ring-seed-pii-fictionalize** (P2, added 2026-07-07) — Do this session. Re-verified live: `public/data/seed.json` still ships "Pamela Ware" and `pam@topspotsfarm.com` to every visitor. Get Nick's yes/no on consent, then fictionalize per the portfolio rule. Static file, no deploy.
- **working-tree-2026-07-10-ring-stale-tree** (P2, added 2026-07-10) — Do this session (reconcile with owner, don't clobber). Confirmed unchanged in 14 days: CHANGELOG, DEPLOY, PROJECT_SPEC, README, public/app.js, src/index.js.
- **canonical-audit-2026-07-12-ring-discord-role-mismatch** (P3, added 2026-07-12) — Do this session. Self-contained: PROJECT_SPEC.md:98 says "Ring role", CLAUDE.md says "Cats" role; confirm the live Discord role and align whichever doc is wrong.

### Cattery
Nine items, now led by a verified-live P0. The money-path and hygiene fixes are drop-in; the scale items stay correctly anticipatory ahead of a zero-breeder launch.

- **canonical-audit-2026-07-02-cattery-missing-migrations** (P1, added 2026-07-02) — Do this session, and note it is two-thirds done. Files for `reviews_and_ratings` (0009) and `waitlists` (0010) now exist but are **untracked**; the third, `admin_set_flags_self_lockout_guard` (ledger 20260630183939), still has no file and is recoverable verbatim from `schema_migrations.statements` (1 statement). The `0006` divergence is confirmed and runs the other way: `0006_photos_storage_bucket.sql` is in the repo but absent from the prod ledger. Four migration files (0009, 0010, 0018, 0019) are sitting untracked — one `git add` plus one ledger recovery closes this.
- **bar-raise-2026-07-08-cattery-webhook-event-loss** (P1, added 2026-07-08) — Do this session. Money-path reliability: mark a Stripe webhook processed only after the DB write succeeds, return 5xx on failure so Stripe retries, and exempt the signature-verified webhook from the 10KB cap.
- **bar-raise-2026-07-08-cattery-money-path-logging** (P1, added 2026-07-08) — Do this session. Add structured `console.error` on every Stripe/RPC failure branch so failed payouts surface in Workers logs; additive, no deploy risk.
- **bar-raise-2026-07-08-cattery-stripe-idempotency-key** (P2, added 2026-07-08) — Do this session, first of the money-path batch. Set an `Idempotency-Key` (order_id+action) on the transfer/refund/checkout POSTs; smallest change, largest downside avoided.
- **bar-raise-2026-07-08-cattery-ratelimit-key-bypass** (P2, added 2026-07-08) — Do this session. Re-key the API throttle on `cf-connecting-ip` (or IP + validated user id); a spoofed Bearer currently escapes it.
- **bar-raise-2026-07-08-cattery-server-side-queries** (P2, added 2026-07-08) — Defer to pre-launch scale. Anticipatory ("before the catalog outgrows the 200-row cap"); nowhere near it with zero listings.
- **bar-raise-2026-07-08-cattery-stuck-escrow-reconcile** (P2, added 2026-07-08) — Defer to the attended money-path session. Three-part build (admin filter + reconciliation sweep + STRIPE.md runbook) needing a deploy, not a drop-in.
- **canonical-audit-2026-07-10-cattery-photos-bucket-listing** (P1, added 2026-07-10) — Do this session. Verified against live prod: the `photos public read` SELECT policy is still on `storage.objects`, so the bucket is still enumerable including photos from unscreened applications. The fix migration `0018_drop_photos_public_listing.sql` is authored, idempotent, and self-documenting — but untracked and never applied. RLS is Cattery's security boundary.
- **canonical-audit-2026-07-16-cattery-orders-cascade-prod-unapplied** (P0, added 2026-07-16) — Do this session, before anything else in Cattery. Verified against live prod: `orders_cattery_id_fkey` reports `confdeltype='c'`, so the auth.users → profiles → catteries → orders chain still hard-deletes held/released/refunded escrow rows — the money-moved audit trail — when a breeder deletes their account. `0019_orders_cattery_fk_restrict.sql` is written and idempotent; it just needs tracking and applying, in the same pass as 0018.

### Skylight
Sixteen items — the largest single backlog, almost all the 2026-07-10 bar-raise (a duplicate-event data-integrity cluster + observability + two refactors). Nearly all are small additive fixes well inside Skylight's additive/low-risk-only rule and bundle into one attended session. Tree is clean this pass, so the co-ownership hazard is low right now — that is an argument for taking the session soon rather than later.

- **canonical-audit-2026-07-02-skylight-google-phase2-framing** (P2, added 2026-07-02) — Do this session. Re-verified: `.project-context.json:79` still frames Google as unshipped "phase 2" while `wrangler.jsonc:48` sets `CALENDAR_DRIVER: "google"` — it is shipped and primary. Doc-accuracy edit, well inside the additive rule.
- **bar-raise-2026-07-10-skylight-data-integrity-01** (P1, added 2026-07-10) — Do this session. The anchor of the duplicate-event cluster: event creation is non-idempotent across all three front doors; derive a deterministic event id from hash(summary+startsAt), pass it to Google, treat 409 as success. Highest-value Skylight item.
- **canonical-audit-2026-07-10-skylight-frame-id-empty-var** (P2, added 2026-07-10) — Do this session (needs a live cred check). Re-verified: `wrangler.jsonc:41` still carries `"SKYLIGHT_FRAME_ID": ""`, which can shadow a same-named secret on deploy. Run `wrangler secret list` to confirm, then drop the vars line.
- **bar-raise-2026-07-10-skylight-data-integrity-02** (P2, added 2026-07-10) — Do this session. Treat a 2xx with an unreadable body as success-with-unknown-id instead of throwing; stops retry-duplication. Pairs with di-01.
- **bar-raise-2026-07-10-skylight-data-integrity-03** (P2, added 2026-07-10) — Do this session. Add Message-ID de-dup via a short-TTL KV set so at-least-once email delivery can't double-book; same cluster.
- **bar-raise-2026-07-10-skylight-data-integrity-04** (P2, added 2026-07-10) — Do this session. Normalize a mixed naive-vs-absolute start/end pair through DEFAULT_TIMEZONE before comparison (or reject); scoped correctness fix.
- **bar-raise-2026-07-10-skylight-observability-01** (P2, added 2026-07-10) — Do this session. Add a `console.error` in the index.ts fetch catch carrying pathname + message; trivial, additive, no PII.
- **bar-raise-2026-07-10-skylight-observability-02** (P2, added 2026-07-10) — Do this session. Wrap the whole inbound-email handler in try/catch that console.errors and best-effort writes a `kind:'error'` KV log; additive.
- **bar-raise-2026-07-10-skylight-observability-03** (P2, added 2026-07-10) — Do this session. Fold a lightweight downstream check into `/api/health` so a slow-burn failure doesn't stay green; additive.
- **bar-raise-2026-07-10-skylight-reliability-03** (P2, added 2026-07-10) — Do this session. On a 401/403 from the Google events POST, delete the KV token, re-mint, retry once; additive self-healing.
- **bar-raise-2026-07-10-skylight-solo-tool-ux-01** (P2, added 2026-07-10) — Do this session. On a 401 from /api/state or /api/quick-add, redirect to sign-in instead of a dead-end "unauthorized"; scoped UX fix.
- **bar-raise-2026-07-10-skylight-dependency-01** (P2, added 2026-07-10) — Promote to scheduled task. The deliverable *is* a standing monitor — a periodic contract-smoke probe against the reverse-engineered lists/chores API that alerts on 4xx. Building it as a queue item and then never scheduling it defeats the point; it belongs in the scheduled-tasks registry alongside the other watches.
- **bar-raise-2026-07-10-skylight-dependency-02** (P2, added 2026-07-10) — Do this session (attended, for the deploy). Bump wrangler from EOL `^3` to `^4`, regenerate the lockfile, smoke-test `wrangler dev` + a deploy; toolchain hygiene that only gets harder.
- **bar-raise-2026-07-10-skylight-maintainability-02** (P2, added 2026-07-10) — Defer to a Skylight refactor. Extracting the ~130-line template-literal client runtime to a typechecked file and deduping esc() touches the live path — heavier than the additive-only bar for this private family tool.
- **bar-raise-2026-07-10-skylight-architecture-01** (P2, added 2026-07-10) — Defer to a Skylight refactor. A canonical event-input type + uniform `createEvent` signature (fixing the Google path silently dropping categoryIds); pairs with maintainability-02 into one refactor session.
- **canonical-audit-2026-07-02-skylight-changelog-phantom** (P3, added 2026-07-16) — Do this session. Re-verified: `.project-context.json:36` sets `"changelog_path": "CHANGELOG.md"` and no such file exists at the repo root. The item's own recommendation (drop the key, since version-lockstep is the deliberate documented convention) is right; it just needs the co-owner's nod because the file is shared.

### Gnosis
Ten items — the two carried items plus the whole 2026-07-16 wave, including the portfolio's only open P0.

- **handler-audit-2026-07-10-gnosis-gate-password-decision** (P1, added 2026-07-10) — Defer to a Nick decision. CLAUDE.md quotes the DM gate password literally; severity is bounded (repo is PRIVATE, the value is a deliberate memorable choice), so this is a keep-documented-vs-scrub judgment call, not an auto-fix. Note it is *not* the P0 below — different surface entirely.
- **canonical-audit-2026-07-10-gnosis-question-status-contract** (P3, added 2026-07-10) — Do this session. Re-verified with corrected paths: the file moved, so the item's `build.mjs:196` is now `web/build.mjs:256` (`QSTATUS = { "?": "open", "x": "answered", "~": "lost" }`), with the "lost to time" chip rendered at `:391` and `:3383`, against PROJECT_SPEC.md:244 ("NEVER closed and carry no ask-tags"). Reconcile the spec to the shipped model.
- **bar-raise-2026-07-16-gnosis-secret-answers-ungated-api** (P0, added 2026-07-16) — Do this session, ahead of every other item in this document. Re-verified open at HEAD `ca3e13f`: `web/worker.js:317` (`GET /api/answers`) and `:320` (`GET /api/answers/export`) both return the full store with no gate check, so the page gate protects the page and nothing else. Eight days and nine releases (v0.3.250 → v0.3.258) have shipped past it, all globe and lore work. Detail deliberately stays in the gitignored local bar-raise doc.
- **bar-raise-2026-07-16-gnosis-release-gate-check-page-only** (P1, added 2026-07-16) — Do this session, in the same pass as the P0. This is *why* the exposure survived every deploy: the check GETs a gated page and asserts 401, which the data path never had to satisfy. Shipping the fix without the assertion leaves the same blind spot for the next regression.
- **bar-raise-2026-07-16-gnosis-uncapped-unauth-kv-writes** (P2, added 2026-07-16) — Do this session. Three unauthenticated POST paths take uncapped free text (one sibling path caps at 120 chars) and the roster accepts the DM as an author, so an anonymous caller can post as the DM. Cap the fields and require the gate cookie for the privileged author; the counting rate limiter can wait for actual abuse.
- **bar-raise-2026-07-16-gnosis-kv-write-failures-swallowed** (P2, added 2026-07-16) — Do this session. Observability is enabled in the wrangler config but nothing structured is logged and several try blocks swallow errors; log KV write failures. Cheap, and it is the same class of blindness that let the P0 run.
- **canonical-audit-2026-07-16-gnosis-public-to-gated-links** (P2, added 2026-07-16) — Do this session. 71 in-prose wikilinks across 40 public pages point at gated URLs — a 14× jump from 5 across 3 pages, driven by the Aestaria ingestion. Not a content leak, but they are dead 401 ends for every player; teach the build's link check to model public→gated as broken so it stops growing.
- **bar-raise-2026-07-16-gnosis-buildmjs-seven-esc-defs** (P2, added 2026-07-16) — Do this session, with the evidence corrected upward. `web/build.mjs` is now **6,345 lines with nine** `esc` definitions (100, 1117, 1367, 2087, 2590, 3599, 3871, 4017, 4097), not seven in 5,332 — three escape `"`, four do not, and two use a DOM textContent round-trip. Hoist one into a shared client preamble. It has grown measurably in eight days and will keep growing.
- **bar-raise-2026-07-16-gnosis-answers-full-store-walk** (P2, added 2026-07-16) — Defer to the pass that follows the P0 fix. The item's own fix shape says "after the security fix scopes the store, serve the public board from a cached projection" — building the cache against the current unscoped store would just have to be redone.
- **canonical-audit-2026-07-16-gnosis-catalog-ogl-provenance** (P3, added 2026-07-16) — Defer to a Kane licensing note. Verified: `web/build.mjs:1360` describes the catalog as open5e OGL data + curated gear and /catalog carries a plain-language attribution, but PROJECT_SPEC has no OGL mention. This is the one place the project ships third-party content publicly, so the wording is Kane's to write, not Claude's to guess.

### Auto-applied this pass

**No item met the unambiguous-Drop bar, so `.work-queue.json` was not modified.**

Every item re-verified this pass is still live, several against production rather than source: Cattery's `orders_cattery_id_fkey` still reports `confdeltype='c'` and the `photos public read` SELECT policy is still on `storage.objects`; Gnosis's `/api/answers` and `/api/answers/export` still answer anonymous callers at HEAD; Budget's two backup directories still hold zero `.db` files; Ring's tree and seed PII and Scheduler's tree are unchanged; the HBH GDD still reads 10 against a measured 11.

Three items came closest to a mutation and none qualified. `handler-audit-skill-table-update-2026-06-14` has a finished staged source but the *installed* skill still reads four handlers, so the drift it exists to close is open — Defer, not Drop. `canonical-audit-2026-07-02-cattery-missing-migrations` is two-thirds recovered but the third migration still has no file and the recovered two are untracked — the item is doing its job. `bar-raise-2026-07-16-gnosis-buildmjs-seven-esc-defs` has stale evidence, but stale in the wrong direction (nine defs, not seven), which is a reason to raise it, not drop it; the id keeps its original name and the correction is recorded here rather than by rewriting the queue entry.

Two items should be *merged* rather than dropped — `handler-audit-2026-07-03-portfolio-discord-webhook-reconcile` and `handler-audit-2026-07-10-webhook-boilerplate` are the same decision counted twice. That merge is a queue edit with a judgment call attached (which id survives), so it is left for Kane rather than auto-applied. Holding all mutations also sidesteps the stale-read clobber a no-push edit would risk against the concurrently-drained queue.
