# Queue triage (rolling)

Single living triage doc for `.work-queue.json`. Supersedes the dated
`QUEUE_TRIAGE-YYYY-MM-DD.md` snapshots and `QUEUE_PIPELINE_DIAGNOSIS-*.md`,
which were folded in here and removed; their point-in-time history remains in
git. The nightly queue-triage task overwrites the "Current state" section below
on each run, so this file always reflects the latest pass rather than spawning a
new dated file.

Last pass: 2026-07-28.
## Current state

**102 non-auto-safe items are aging past 7 days** (up one from last pass), alongside **55 items escalated to Kane** at scan time. Every one of the 55 was re-verified against the repos, the live sites and live prod this pass: **52 hold, 3 are resolved**. The escalated count now reads **53**, not 52, because a concurrent Cattery audit filed one new escalation mid-run (`canonical-audit-2026-07-28-cattery-commit-message-false-provenance`, covered below). The worst offender by age in the aging set is `canonical-audit-2026-07-02-skylight-changelog-phantom` at **12 days**; in the escalated set the three completion gates (`yac-store-launch`, `yac-subscriptions-enable-ai`, `hbh-asset-production-pass`) hit **48 days**. Largest aging backlog is **Hordes at 32**, then **Everything at 25** and **Rising at 17**; largest escalated backlog is **Skylight at 15**.

**The Gnosis P0 is closed - the first exposure in this queue to be fixed rather than re-verified.** Last pass reproduced an unauthenticated GET of `https://gnosis.yesandeverything.com/api/answers` returning 200 and 380,190 bytes with DM-tagged records. Today the same GET returns **200 / 95,966 bytes / 182 records / 0 DM-tagged**, and `web/worker.js:319-329` shows why: `/api/answers` now filters the secret set without a DM session, and `/api/answers/export` requires a DM cookie or the gate password. Marked `completed`.

**The other P0 is unchanged, and chasing its evidence turned up a false commit message.** Cattery prod (`emrerjdyujylgutfmool`) still reports `orders_cattery_id_fkey confdeltype='c'` and still carries the `photos public read` storage policy. The *untracked* half of that finding is fixed - `0018` and `0019` were committed in `9f7fdf6`. But that commit is titled "recover 4 file-less prod migrations **from the ledger**", and the ledger does not contain them: `supabase_migrations.schema_migrations` holds 17 entries ending at `20260704173944` (= disk `0017`), with no `0018` or `0019` anywhere in it. The two files were authored, not recovered, and have never been applied. A concurrent Cattery audit reached the same conclusion independently during this run and filed `canonical-audit-2026-07-28-cattery-commit-message-false-provenance` (P1/high) - two separate passes converging on it is worth weighting. Both Cattery live items are now purely a `supabase db push` away.

**One item self-healed for the second time and was re-escalated in between.** `canonical-audit-2026-07-10-skylight-frame-id-empty-var` was unblocked by last night's triage, re-escalated by the 2026-07-27 hourly drain, and is being unblocked again today on firmer evidence (`wrangler.jsonc:45` now carries a populated value). If the drain re-escalates it a third time, the escalation predicate is the defect, not the item.

**Three findings are worse than filed.** The Skylight `yac-skylight-audit` Windows task is not dormant - it ran **2026-07-26** and failed with result 1, so it has been failing nightly rather than sitting idle. Gnosis `build.mjs` now defines `esc()` **nine** times, up from seven. And Rising's Discord webhook provisioning has *regressed* to **0 of 5** from the 4 the boilerplate finding recorded.

### Escalated to Kane (blocked / blocked-on-user)

- **bar-raise-2026-07-24-hordes-gate-page-publish-01** (`blocked-on-user`, P1, added 2026-07-24) - re-verified live GET of https://yesandeverything.com/hordes/ - 200, 2,226,762 bytes, all three of `var PASSWORD` / `var EDITOR` / `var ENCODED` still in the unauthenticated body, byte-identical to last pass. **Unblocks when:** Kane rules whether /hordes/ becomes a server-side gate or is re-scoped as public - the literals cannot be scrubbed without that call.
- **bar-raise-2026-07-24-hordes-security-03** (`blocked-on-user`, P1, added 2026-07-24) - re-verified same live GET; `var PASSWORD = 'SneakPeak'` still at the published `hordes/index.html:144`. Note `publish-gdd.ps1` no longer contains the literal - it is the committed page that carries it, so the finding's cited emit site has moved. **Unblocks when:** the same ruling as gate-page-publish-01 - these two are one decision and should be closed together.
- **bar-raise-2026-07-24-scheduler-gate-pages-v041-prose** (`blocked-on-user`, P2, added 2026-07-24) - re-verified `projects/scheduler/index.html` - only the `live:version` pill (v0.7.1) mentions a version; no M7.1, v0.6.0 per-seat billing, or migration 0009-0011 prose anywhere in the page. **Unblocks when:** Kane answers the sibling posture-decision item - whether Scheduler is being driven or parked decides what the page should claim.
- **bar-raise-2026-07-24-scheduler-posture-decision** (`blocked-on-user`, P2, added 2026-07-24) - re-verified no locked decision recorded in `docs/DESIGN.md` section 23; the doc's own header still logs docs-only correction passes through 2026-07-28 with no posture entry. **Unblocks when:** Kane answers: driven to a first real user now, or parked at v0.7.x.
- **canonical-audit-2026-07-20-cattery-projectspec-stale-sibling-name** (`blocked-on-user`, P3, added 2026-07-20) - re-verified `PROJECT_SPEC.md` still names 'Yes& Cats' / cats.yesandeverything.com at :24, :27, :29 and the locked line :98 (line numbers drifted from the finding's :20/:23/:25/:92). **Unblocks when:** Kane confirms 'Yes& Cats' was renamed to Yes& Ring rather than superseded by it - the line is locked, so it cannot be swapped by inference.
- **handler-audit-2026-07-17-skylight-audit-task-husk** (`blocked-on-user`, P2, added 2026-07-17) - re-verified Windows task `yac-skylight-audit` via `Get-ScheduledTask`: State=Ready, LastRunTime=**2026-07-26 06:30**, LastTaskResult=**1**. Worse than filed - the finding said it had not run since 07-12; it runs daily and fails daily. **Unblocks when:** Kane says whether the legacy Windows task is retired in favour of the native routine or repaired - it is failing every night either way.
- **handler-audit-2026-07-17-skill-itself-stale** (`completed`, P2, added 2026-07-17) - re-verified **RESOLVED**. The rewrite this item asked for was already written on 2026-07-26. `_skill-review/personal-skills-src/skills/handler-audit/SKILL.md` (17,101 bytes) has zero matches for `HereThereBeHordes`, `X:\Scheduler` or `HTBH`, and names all **twelve** handler paths correctly. Both filed defects are gone at source. What is still live in-session is the stale *packaged* artifact - `_skill-review/handler-audit.skill` is dated **2026-06-11** (6,087 bytes), 45 days behind its source, which is why the loaded skill description still says HTBH and four handlers. That residue belongs to `bar-raise-2026-07-17-yae-yae-skill-artifacts-stale-vs-source`, which owns repackaging and now has this as its sharpest instance. **Unblocks when:** nothing - marked `completed` this pass.
- **bar-raise-2026-07-17-chains-compliance-02** (`blocked-on-user`, P1, added 2026-07-17) - re-verified `legal/terms.md` - zero case-insensitive matches for subscription, billing, or refund. The Cancel-anytime modal copy is still unbacked. **Unblocks when:** Kane names the refund policy and billing cadence - counsel-grade terms cannot be drafted from the code.
- **bar-raise-2026-07-17-chains-compliance-03** (`blocked-on-user`, P1, added 2026-07-17) - re-verified `_DRAFT_BANNER_HTML` still defined at `src/legal-view.ts:295` and injected into both the served Terms (:316) and Privacy (:321). **Unblocks when:** compliance-02 lands - removing the DRAFT banner before the payment clause exists would make the Terms worse, not better.
- **bar-raise-2026-07-16-yab-dependency-02** (`blocked-on-user`, P1, added 2026-07-16) - re-verified `gh api repos/TheProphetKane/yesandbudget/vulnerability-alerts` -> 404 'Vulnerability alerts are disabled'; `automated-security-fixes` -> `{"enabled":false}`. The doc half was already corrected in a962350. **Unblocks when:** Kane toggles Dependabot alerts on in the repo settings - it is a GitHub UI action no routine holds a token for.
- **bar-raise-2026-07-16-yab-data-integrity-02** (`blocked-on-user`, P1, added 2026-07-16) - re-verified `apps/api/src/plaid/sync.ts:102` still `ON CONFLICT(plaid_transaction_id) DO NOTHING` against the partial index created at `apps/api/src/db.ts:293` (`WHERE plaid_transaction_id IS NOT NULL`). **Unblocks when:** an attended Budget session runs `scripts/release.ps1` with this and `finance-product-dedupe-04` in the same commit - shipping either alone trades one bug for another.
- **canonical-audit-2026-07-16-cattery-orders-cascade-prod-unapplied** (`blocked-on-user`, P0, added 2026-07-16) - re-verified **live prod query today**: `orders_cattery_id_fkey confdeltype='c'` - still ON DELETE CASCADE. The untracked half is now FIXED (0018 and 0019 were committed in `9f7fdf6`), but `supabase_migrations.schema_migrations` tops out at version `20260704173944`, so neither has ever been applied. **Unblocks when:** an attended Cattery session with Supabase write access runs `supabase db push` (or the SQL editor) against prod - the repo half is done and this is now purely a DDL apply.
- **bar-raise-2026-07-16-ring-verdict-enum-stale** (`blocked-on-user`, P3, added 2026-07-16) - re-verified `scripts/write-dashboard-status.ps1:96` still `@('healthy','needs-attention','at-risk','stalled')`; `status/data/Ring.json` still carries verdict `working`, which the next writer run nulls. **Unblocks when:** an attended Ring session ships the one-line enum change through `release.ps1` - the fix has no value unreleased, which is why the no-publish drain keeps declining it.
- **bar-raise-2026-07-16-cattery-verdict-enum-stale** (`blocked-on-user`, P3, added 2026-07-16) - re-verified `scripts/write-dashboard-status.ps1:83` still the old four-value list; `status/data/Cattery.json` carries `needs-attention`, which *is* in the enum - so unlike Ring this one is not actively nulling anything today, it is a latent trap. **Unblocks when:** same attended release as the Ring twin - pair them, they are one edit in two repos.
- **bar-raise-2026-07-16-gnosis-secret-answers-ungated-api** (`blocked-on-user`, P0, added 2026-07-16) - re-verified **RESOLVED.** `web/worker.js:319-329` now filters `SECRET_Q` out of `/api/answers` without a DM session and `/api/answers/export` requires a DM cookie or the gate password. Live unauthenticated GET returns 200 / **95,966 bytes / 182 records / 0 DM-tagged**, against 380,190 bytes with `"c":"DM"` records last pass. **Unblocks when:** nothing - marked `completed` this pass.
- **gnosis-release-62-proposed-dm-answers-2026-07-16** (`blocked-on-user`, P1, added 2026-07-16) - re-verified `git merge-base --is-ancestor 74ba52b HEAD` still returns false at Gnosis HEAD `3ddfdf2` - the four residual answers are still stranded on the unmerged commit. **Unblocks when:** Kane gives an attended 'go' - the item ends in a Gnosis release, which no routine may run.
- **hbh-autopause-horde-day-signal-2026-07-14** (`blocked-on-user`, P1, added 2026-07-14) - re-verified all four legs still true: `auto_pause.gd:88` guards `has_signal("horde_day_announced")`, `world3d/wave_director.gd:7` declares only `all_waves_cleared`, and no file outside `auto_pause.gd` references the autoload. **Unblocks when:** sibling `bar-raise-2026-07-17-hordes-playability-02` lands first - pause has to become authoritative before reviving the thing that calls it is worth anything.
- **canonical-audit-2026-07-12-ring-discord-role-mismatch** (`blocked-on-user`, P3, added 2026-07-12) - re-verified still contradictory: `PROJECT_SPEC.md:102` says 'Ring role', `CLAUDE.md:79` says the channels grant the '**Cats**' role. **Unblocks when:** Kane (or anyone with the Discord open) reads which role the 🐱 Yes and... Ring category actually grants - the repo cannot answer it.
- **bar-raise-2026-07-10-yab-plaid-takeover-window** (`blocked-on-user`, P1, added 2026-07-10) - re-verified `apps/web/src/components/PlaidConnectFlow.tsx:90-93` still hard-codes 90 days with the 'Phase 2 should ask Plaid for the real earliest_available_date' comment intact. **Unblocks when:** Plaid's `earliest_available_date` is wired in, or Kane accepts the 90-day window as permanent and the comment gets closed as a locked decision.
- **handler-audit-2026-07-10-gnosis-gate-password-decision** (`blocked-on-user`, P1, added 2026-07-10) - re-verified narrowed: `CLAUDE.md` and `PROJECT_SPEC.md` no longer match `GATE_PASSWORD` at all. `web/worker.js:8` still names the value in a comment. `gh repo view` confirms the repo is private. **Unblocks when:** Kane picks the fork: accept the literal in a private repo and drop the .secrets framing, or scrub and rotate `GATE_PASSWORD` via wrangler.
- **handler-audit-2026-07-10-webhook-boilerplate** (`blocked-on-user`, P2, added 2026-07-10) - re-verified re-counted `X:\.secrets\<Project>\scripts\.discord_*.txt` today: Hordes 1, Chains 1, Scheduler 3, Apothecary 3, Budget 4, Rising **0**, Ring 3, Cattery 0, Skylight 0, Agents 0, Gnosis 6, Everything 0. Rising has *regressed* from the 4 the finding recorded. **Unblocks when:** Kane decides whether the five-webhook promise is the standard (provision the missing ones) or the handlers should describe what each project actually has.
- **scheduler-auditlog-composite-index-2026-07-10** (`blocked-on-user`, P3, added 2026-07-10) - re-verified **decision half resolved**: `migrations/0011_audit_log_composite_index.sql:10` creates `idx_audit_log_org_created ON audit_log(organization_id, created_at)` and `docs/DESIGN.md:367` documents it. What remains is applying it. **Unblocks when:** an attended Scheduler release runs migration 0011 against prod D1.
- **canonical-audit-2026-07-10-cattery-photos-bucket-listing** (`blocked-on-user`, P1, added 2026-07-10) - re-verified **live prod query today**: the `photos public read` SELECT policy on `storage.objects` is still present in `pg_policies`. Repo-side `0018` is authored and now committed; only the apply is missing. **Unblocks when:** the same attended `supabase db push` that lands 0019 - these two are one apply, not two.
- **canonical-audit-2026-07-10-skylight-frame-id-empty-var** (`blocked-on-user`, P2, added 2026-07-10) - re-verified **RESOLVED.** `wrangler.jsonc:45` is now `"SKYLIGHT_FRAME_ID": "4526946"` - a populated plain var, not the empty binding the finding cites. That answers the var-or-secret fork the item was gated on. `README.md:71` and `src/index.ts:141` still instruct `wrangler secret put`, which is now the only open half and is doc-only. **Unblocks when:** nothing - returned to `pending` this pass. NOTE: last pass also unblocked this and the 2026-07-27 hourly drain re-escalated it; if that recurs, the drain's escalation predicate is the bug, not the item.
- **canonical-audit-2026-07-10-gnosis-question-status-contract** (`blocked-on-user`, P3, added 2026-07-10) - re-verified still contradictory: `web/build.mjs:256` defines `QSTATUS = { '?': 'open', 'x': 'answered', '~': 'lost' }` while `PROJECT_SPEC.md:244` says questions 'are NEVER closed and carry no ask-tags (locked 2026-07-06)'. **Unblocks when:** Kane rules which side wins - the spec line is locked, so the standing 'code disagrees with the spec' rule cannot resolve it unilaterally.
- **hbh-velocity-off-critical-path-mission-layer-2026-07-10** (`blocked-on-user`, P1, added 2026-07-10) - re-verified `source/data/mission.gd` and `source/data/missions/` still do not exist at HBH HEAD. **Unblocks when:** Kane commits to a mission-layer milestone, or accepts that M3 First Playable stays Partial while art work continues - the item is a priority call, not a bug.
- **bar-raise-2026-07-10-skylight-dependency-01** (`blocked-on-user`, P2, added 2026-07-10) - re-verified no contract smoke probe exists; `src/skylight.ts` still rides the inferred reverse-API bodies. **Unblocks when:** an attended Skylight session - additive-only rule makes a new probe safe, it just needs a hand on the wheel.
- **bar-raise-2026-07-10-skylight-observability-01** (`blocked-on-user`, P2, added 2026-07-10) - re-verified **zero** `console.*` calls anywhere in `src/` (grep count 0), against `wrangler.jsonc` observability enabled. **Unblocks when:** same attended Skylight session - this is the cheapest of the fifteen and should lead the batch.
- **bar-raise-2026-07-10-skylight-maintainability-02** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `esc()` still defined twice - `src/ui.ts:128` (inside a JS template-literal string) and `:393` (TS). **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-architecture-01** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `categoryIds` appears only in `src/skylight.ts:148,161` and nowhere in `src/google.ts`; `src/types.ts` defines no canonical event input. **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-reliability-03** (`blocked-on-user`, P2, added 2026-07-10) - re-verified no 401/403 handling and no `g:token` delete-and-retry in `src/google.ts` - the only 401/403 mention is a comment at :141. **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-data-integrity-02** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `src/google.ts:78` and `:131` still `await res.json()` unguarded after the write commits; `src/skylight.ts:68` same shape (`:129` has since gained a `.catch`). **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-observability-02** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `src/email.ts` MIME parsing still runs outside the handler try; the handler is still fire-and-forget under `ctx.waitUntil`. **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-observability-03** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `src/index.ts:80-82` - `/api/health` still returns `{ok:true, version, now}` unconditionally with the comment 'exposes nothing but the version'. **Unblocks when:** an attended Skylight session, or Kane points an external uptime probe at `/api/setup` instead.
- **bar-raise-2026-07-10-skylight-data-integrity-03** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `Message-ID` is read at `src/email.ts:108` but only to set `In-Reply-To`/`References` for threading - there is still no dedupe set. **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-solo-tool-ux-01** (`blocked-on-user`, P2, added 2026-07-10) - re-verified zero occurrences of `401` in `src/ui.ts` - an expired cookie still dead-ends. **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-10-skylight-dependency-02** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `package.json:20` still `"wrangler": "^3.60.0"` - the EOL v3 major. **Unblocks when:** an attended Skylight session; this one needs a deploy smoke-test, so it should go last in the batch.
- **bar-raise-2026-07-10-skylight-data-integrity-04** (`blocked-on-user`, P2, added 2026-07-10) - re-verified `src/dates.ts:33-37` `epochOf` still anchors naive values to UTC and parses absolute forms as true instants, and `src/router.ts` compares them directly. **Unblocks when:** an attended Skylight session.
- **bar-raise-2026-07-08-cattery-webhook-event-loss** (`blocked-on-user`, P1, added 2026-07-08) - re-verified both halves hold and one is worse than filed: `src/index.js` marks the event seen via `srv_event_seen` *before* any DB write, and the 10KB cap at `:411` still applies to `/api/stripe/webhook` - the exemption at `:412` covers only content-type and rate limiting, though the comment above it claims the webhook is exempt. **Unblocks when:** an attended Cattery money-path session with a `wrangler deploy`.
- **bar-raise-2026-07-08-cattery-money-path-logging** (`blocked-on-user`, P1, added 2026-07-08) - re-verified **zero** `console.error` calls in `src/index.js` - every Stripe and RPC failure branch is still silent. **Unblocks when:** the same attended Cattery money-path session - this is the cheapest of the four and should lead.
- **bar-raise-2026-07-08-cattery-stripe-idempotency-key** (`blocked-on-user`, P2, added 2026-07-08) - re-verified zero occurrences of `Idempotency-Key` in `src/index.js`. **Unblocks when:** the same attended Cattery money-path session.
- **bar-raise-2026-07-08-cattery-ratelimit-key-bypass** (`blocked-on-user`, P2, added 2026-07-08) - re-verified `src/index.js:417` still `const key = auth ? auth.slice(-40) : (req.headers.get("cf-connecting-ip") || "anon")` - an unvalidated Bearer fragment still wins over the IP. **Unblocks when:** the same attended Cattery money-path session.
- **bar-raise-2026-07-07-ring-worker-observability** (`blocked-on-user`, P1, added 2026-07-07) - re-verified 18 bare/empty catch blocks remain in `src/index.js`; `wrangler.jsonc:11` still `observability.enabled: true` with no `logpush` or `tail_consumer` sink. **Unblocks when:** one batched attended Ring deploy session - this item and the four Ring siblings below are all gated on the same `release.ps1` + `wrangler deploy`.
- **bar-raise-2026-07-07-ring-community-ratings-backup** (`blocked-on-user`, P1, added 2026-07-07) - re-verified `supabase/migrations/` still tops out at `0007_user_state_rls.sql` - no 0008, so judge ratings are still KV-only. **Unblocks when:** the same batched attended Ring deploy session.
- **bar-raise-2026-07-07-ring-dualwrite-atomicity** (`blocked-on-user`, P1, added 2026-07-07) - re-verified no repair-on-read or reconcile path added; the dual-write is unchanged. **Unblocks when:** the same batched attended Ring deploy session.
- **bar-raise-2026-07-07-ring-worker-freetier-budget** (`blocked-on-user`, P1, added 2026-07-07) - re-verified cron fan-out and typeahead fan-out unchanged in `src/index.js`. **Unblocks when:** the same batched attended Ring deploy session - though this one is a growth risk, not a live failure, and could reasonably be deferred out of the batch.
- **bar-raise-2026-07-07-ring-gdpr-data-endpoints** (`blocked-on-user`, P1, added 2026-07-07) - re-verified zero matches for `/api/user/data` or `/api/user/export` in `src/index.js`. **Unblocks when:** the same batched attended Ring deploy session.
- **bar-raise-2026-07-07-ring-seed-pii-fictionalize** (`blocked-on-user`, P2, added 2026-07-07) - re-verified `public/data/seed.json` still ships real identities to every visitor - `Pamela Ware / Alexander Abbott` at :36-37 and `pam@topspotsfarm.com` at :82. **Unblocks when:** Kane says whether this is consented seed data (document the exception) or not (fictionalize it) - this is the one Ring item that is a decision, not a deploy.
- **chains-br-auth-01-2026-07-04** (`blocked-on-user`, P1, added 2026-07-04) - re-verified `worker/src/lib.js:1749-1750` - `_requireEntitledUser` still reads `body.userId` with only a `typeof === 'string'` check (line numbers drifted from the filed :1717). **Unblocks when:** an attended Chains session runs `release.ps1` plus a worker deploy and an end-to-end auth test - this is the highest-value single item in the escalated set.
- **bar-raise-2026-07-04-yab-bulk-delete-no-backup** (`blocked-on-user`, P1, added 2026-07-04) - re-verified `apps/api/src/server.ts:532-542` - the bulk-delete handler still goes straight to `DELETE FROM transactions` with no `createBackup`, while `:1409` and `:1423` show the takeover/restore paths taking one. **Unblocks when:** an attended Budget session runs `scripts/release.ps1`.
- **canonical-audit-2026-07-02-skylight-google-phase2-framing** (`blocked-on-user`, P2, added 2026-07-02) - re-verified `.project-context.json:79` still reads 'Skylight reverse API ... is phase 1; Google Calendar API is phase 2' while the code defaults `CALENDAR_DRIVER` to google. **Unblocks when:** an attended Skylight session - fold it into the batch of fifteen, it is the cheapest doc-only member.
- **hbh-multimesh-crowd-migration-2026-06-28** (`blocked-on-user`, P1, added 2026-06-28) - re-verified `source/world3d/crowd3d.gd` exists and is still unwired - `world_3d.gd:213` instantiates `EnemyField3D` and never references `Crowd3D`. **Unblocks when:** Kane greenlights an attended Godot perf session; the HBH release + GDD pill rules put this out of any routine's reach.
- **yac-store-launch** (`blocked-on-user`, P1, added 2026-06-10) - re-verified no change - still one of the two gates between 92% and done. **Unblocks when:** Kane decides Chains is feature-complete enough to list; the six open Chains findings (auth-01, both compliance items, dataint-01) are arguably prerequisites to putting it in a store.
- **yac-subscriptions-enable-ai** (`blocked-on-user`, P1, added 2026-06-10) - re-verified `src/caddy.ts:62` still `export const CHAINS_VISIBLE = false`. **Unblocks when:** subscriptions billing ships, then Kane flips Decision 28 - and `compliance-02` should land first so the terms cover what is being billed.
- **hbh-asset-production-pass** (`blocked-on-user`, P1, added 2026-06-10) - re-verified no change - 48 days, the oldest item in the queue. **Unblocks when:** Kane queues the Midjourney batches; every downstream HBH art-gated item is waiting behind this one.

### Hordes

- **bar-raise-2026-07-17-hordes-asset-pipeline-02** (P2, added 2026-07-17) - Do this session. 51 of 55 tracked .glb never checked, and the `.import` sidecar check is the one that has actually bitten before (see the Navy art-integration trap).
- **bar-raise-2026-07-17-hordes-audit-loop-integrity-01** (P2, added 2026-07-17) - Do this session. Recording that HBH's canonical doc is release-gated is what stops the nightly re-finding work it is structurally barred from doing.
- **bar-raise-2026-07-17-hordes-audit-loop-integrity-02** (P2, added 2026-07-17) - Do this session. 0/0/0 as a permanent fixed point means the Hordes dashboard audit numbers have never been real.
- **bar-raise-2026-07-17-hordes-compliance-data-steward-01** (P2, added 2026-07-17) - Do this session. Godot's logo is CC BY 4.0 and Electrolize is OFL - both are shipping with the attribution file deleted.
- **bar-raise-2026-07-17-hordes-cost-economics-02** (P1, added 2026-07-17) - Promote to scheduled task. 3.09x spend against a third of the commits, and a frozen tree still burning ~$6/day. Attribution has to be continuous to be worth anything - pairs with the YaE cost item.
- **bar-raise-2026-07-17-hordes-data-integrity-01** (P2, added 2026-07-17) - Do this session. Save/load erases the in-flight horde while restoring wave_director state - saving on the final wave and reloading is an unwinnable state.
- **bar-raise-2026-07-17-hordes-dependency-01** (P2, added 2026-07-17) - Do this session. A git-tracked, enabled editor plugin with no license and no provenance row, falsifying ASSET_PROVENANCE.md.
- **bar-raise-2026-07-17-hordes-incident-runbook-currency-01** (P1, added 2026-07-17) - Do this session. An XSS sink and an unauthenticated write, both waiting on a deploy that never ran.
- **bar-raise-2026-07-17-hordes-incident-runbook-currency-02** (P2, added 2026-07-17) - Do this session. A runbook that orphans a live KV namespace when followed literally is worse than no runbook.
- **bar-raise-2026-07-17-hordes-maintainability-01** (P1, added 2026-07-17) - Do this session. A 778-line research autoload with zero live callers while a 5-tech stand-in runs the game, and Mass Fabrication cap bonuses are permanently 0 as a result. That last part is a live gameplay bug, not just duplication.
- **bar-raise-2026-07-17-hordes-maintainability-04** (P2, added 2026-07-17) - Do this session. Stale decomposition numbers plus a false 'no remaining violations' claim - the false claim is the part that matters.
- **bar-raise-2026-07-17-hordes-per-project-ps1-stack-01** (P1, added 2026-07-17) - Do this session. A prose-only worker deploy against a Chains release.ps1 block that is intact and copyable.
- **bar-raise-2026-07-17-hordes-playability-01** (P2, added 2026-07-17) - Defer to the mission-layer milestone Kane commits to. Chronic x7 and duplicate in substance with the escalated `hbh-velocity-off-critical-path-mission-layer` item; it should not be tracked twice.
- **bar-raise-2026-07-17-hordes-playability-02** (P1, added 2026-07-17) - Do this session. Pause not being authoritative is the blocker under the escalated auto_pause item; nothing else in the Hordes slice moves until this does.
- **bar-raise-2026-07-17-hordes-playability-03** (P2, added 2026-07-17) - Do this session. Five of eight hotkeys charge a different price than the menu shows; that is a player-visible correctness bug, not a refactor.
- **bar-raise-2026-07-17-hordes-reliability-02** (P1, added 2026-07-17) - Do this session. Four orphaned economy recorders with no callers - wire or delete, but decide.
- **bar-raise-2026-07-17-hordes-reliability-03** (P2, added 2026-07-17) - Defer to the M3 First Playable push. Autosave matters once there is a run worth losing; the mission layer does not exist yet.
- **bar-raise-2026-07-17-hordes-save-format-stability-02** (P2, added 2026-07-17) - Do this session. `world3d_save.json` already has the temp-write-readback-rename pattern; copying it to `options.tres` is mechanical.
- **bar-raise-2026-07-17-hordes-scale-architecture-01** (P2, added 2026-07-17) - Defer to the next HBH performance pass. O(R^3) per unit per frame is real, but it should be measured against the multimesh migration rather than optimised in isolation.
- **bar-raise-2026-07-17-hordes-scheduled-task-firing-state-01** (P2, added 2026-07-17) - Do this session. One line, mirroring `audit-apothecary-nightly:15`.
- **bar-raise-2026-07-17-hordes-security-01** (P1, added 2026-07-17) - Do this session. Unvalidated KV values into an innerHTML sink at two GDD sites; inert only by accident.
- **bar-raise-2026-07-17-hordes-security-02** (P2, added 2026-07-17) - Do this session. Origin-less POSTs skip the allowlist entirely - a public unauthenticated write on a live Worker.
- **bar-raise-2026-07-17-hordes-skill-chain-handoff-integrity-01** (P1, added 2026-07-17) - Do this session. The dedupe bug that swallows re-found open findings - it corrupts every future bar-raise run, so it compounds daily. `drift-auto-fix/SKILL.md:142` already has the wording to copy.
- **bar-raise-2026-07-17-hordes-skill-suite-health-01** (P1, added 2026-07-17) - Do this session. Blocked-on-stale-packaging; pairs with the YaE `skill-artifacts-stale-vs-source` item, which is the actual fix.
- **bar-raise-2026-07-17-hordes-skill-suite-health-02** (P1, added 2026-07-17) - Do this session. A hazard catalog describing the pre-v0.98.5 2D codebase is actively misleading every code-audit run against HBH.
- **bar-raise-2026-07-17-hordes-skill-trigger-collisions-01** (P2, added 2026-07-17) - Do this session. Chronic x7. Two registered bar-raise skills routing correctly by luck - a fully-qualified id is a one-line edit and it keeps re-appearing because nobody makes it.
- **bar-raise-2026-07-17-hordes-solo-dev-voice-portfolio-01** (P1, added 2026-07-17) - Do this session. The memory-key dump is still in the public GDD changelog and `check_gdd_voice.py` still has zero patterns for the AI tool names. The guard half is the part with forward value.
- **bar-raise-2026-07-17-hordes-voice-and-tone-01** (P2, added 2026-07-17) - Defer to the next HBH content pass. Renaming six research nodes is authorial work Kane should do in one sitting with the Order 42 frame in front of him, not piecemeal.
- **bar-raise-2026-07-17-hordes-work-queue-hygiene-01** (P2, added 2026-07-17) - Do this session. Nine open HBH items, none drainable - this is the meta-item that explains why Hordes is the largest backlog in this report.
- **hbh-gdd-path-extends-phrasing-2026-07-17** (P3, added 2026-07-17) - Do this session. A retired convention still named in the GDD; source has zero instances.
- **hbh-gdd-settings-backbone-stale-2026-07-17** (P2, added 2026-07-17) - Do this session. GDD describes shipped settings as future work and points at two wrong paths - doc-only, but it ends in a release pill bump, which is why the nightly cannot take it.
- **hbh-orphan-mainmenu-settings-deadcode-2026-07-17** (P3, added 2026-07-17) - Do this session. Re-verified today: `source/ui/main_menu/settings.gd` + `.tscn` reference only each other - a self-contained orphan pair, safe to delete.

### Rising

- **bar-raise-2026-07-17-rising-architecture-01** (P2, added 2026-07-17) - Do this session. A hardcoded six-name god-file guard that is blind to the four current offenders - a ratchet that does not ratchet.
- **bar-raise-2026-07-17-rising-compliance-data-steward-01** (P2, added 2026-07-17) - Do this session. Spine licensing and Navy's commissioned-art terms are exactly the things that are hard to reconstruct later; record-what-is-true, UNKNOWN where unknown.
- **bar-raise-2026-07-17-rising-compliance-data-steward-03** (P2, added 2026-07-17) - Do this session. A hard-rule check that always fails is a gate that never blocks; rescope it to `source/`.
- **bar-raise-2026-07-17-rising-data-integrity-02** (P2, added 2026-07-17) - Do this session. A save round-trip test plus a corrupt-JSON negative case; pairs with data-integrity-03 below.
- **bar-raise-2026-07-17-rising-data-integrity-03** (P2, added 2026-07-17) - Do this session. Bounds-validate restored tiles before instantiation; pairs with game-design-02, do them together.
- **bar-raise-2026-07-17-rising-game-design-02** (P2, added 2026-07-17) - Do this session. A grid-size mismatch that warns and restores anyway places entities out of bounds; the comment already flags it as deferred.
- **bar-raise-2026-07-17-rising-game-design-03** (P2, added 2026-07-17) - Do this session. Inherited HBH version stamps and a stale render-scale header; note the memory rule that the 58 v0.60-v0.65 stamps are a known port artefact, not drift - only the cited `save_load.gd:382` case and the unit.gd header are in scope.
- **bar-raise-2026-07-17-rising-maintainability-01** (P2, added 2026-07-17) - Do this session. Two sections describing pre-decomposition god-files in the present tense when all six are now under 1000 lines.
- **bar-raise-2026-07-17-rising-maintainability-02** (P2, added 2026-07-17) - Do this session. ARCHITECTURE.md is two decomposition waves behind and carries a Scout base contradiction.
- **bar-raise-2026-07-17-rising-maintainability-03** (P2, added 2026-07-17) - Do this session. A dead ASSET_SCALE_MASTER read contradicting the locked no-scale-constants decision - and stale docs are precisely how that constant crept back in before.
- **bar-raise-2026-07-17-rising-observability-02** (P2, added 2026-07-17) - Do this session. Same 0/0/0 fixed-point bug as the Hordes audit-loop item - two projects, one root cause, fix them with one pattern.
- **bar-raise-2026-07-17-rising-orchestration-03** (P2, added 2026-07-17) - Do this session. Re-verified today: `personal-skills-src/skills/code-audit/hazards/per-project/BR.md` does not exist anywhere on disk. Repoint or remove the field.
- **bar-raise-2026-07-17-rising-performance-01** (P2, added 2026-07-17) - Do this session. The optimization log predates both the decomposition and the 256px art drop, so every later performance claim has no baseline to stand on.
- **bar-raise-2026-07-17-rising-performance-04** (P2, added 2026-07-17) - Do this session. The spatial bucket index already exists - routing target acquisition through it is using what is already built.
- **bar-raise-2026-07-17-rising-public-voice-01** (P2, added 2026-07-17) - Do this session. Two em-dash entities on the YaE-side gate page. Note the fix lands in YesAndEverything, and the item explicitly authorises that crossing.
- **bar-raise-2026-07-17-rising-reliability-02** (P2, added 2026-07-17) - Do this session. These three functions regressed against each other twice already - a deterministic contract test is the specific fix for the specific history.
- **bar-raise-2026-07-17-rising-solo-tool-ux-01** (P2, added 2026-07-17) - Do this session. This is the root of the recurring `release.ps1` stderr-abort that shows up in the memory notes for three separate projects - fixing it here is the template for the rest.

### Chains

- **bar-raise-2026-07-17-chains-cost-01** (P2, added 2026-07-17) - Do this session. Two endpoints missing the spend guard the other AI paths already have - copy the existing guard, no design decision needed.
- **bar-raise-2026-07-17-chains-dataint-01** (P1, added 2026-07-17) - Do this session. Entitlement reconciliation is the one Chains finding that silently takes money from a user and gives nothing back; it belongs in the same attended session as auth-01.
- **bar-raise-2026-07-17-chains-dataint-02** (P2, added 2026-07-17) - Defer to the attended Chains worker session that lands auth-01. KV read-modify-write on the spend counters is structural and needs a storage decision, not a patch.
- **bar-raise-2026-07-17-chains-finance-02** (P2, added 2026-07-17) - Do this session. A `past_due` branch is a small addition to two call sites, and undefined renewal behaviour on a paid product is the kind of thing that surfaces as a support ticket.
- **bar-raise-2026-07-20-chains-install-offline-01** (P2, added 2026-07-20) - Defer to the next Chains feature milestone. Persisting course detail for offline is a real product gap but it is a feature, not a defect, and it competes with the four open money-path items.
- **bar-raise-2026-07-20-chains-sync-01** (P2, added 2026-07-20) - Do this session. Tombstone-merge already exists for rounds; extending it to bags/discs/collection is a copy of a proven pattern, and silent row deletion is data loss.

### Apothecary

- **bar-raise-2026-07-17-apothecary-comment-accuracy-sweep** (P3, added 2026-07-17) - Do this session. Two rows added to a CLAUDE.md table; the only Apothecary item in the queue and the cheapest thing in this report.

### Budget

- **bar-raise-2026-07-16-cross-budget-openfindings-backfill** (P2, added 2026-07-16) - Do this session. Parts 1 and 2 already shipped; the historical backfill is the deliberately-deferred remainder and it is mechanical.
- **bar-raise-2026-07-16-yab-finance-product-categorization-tail-01** (P1, added 2026-07-16) - Do this session. Chronic, runsOpen 7 - the tail never shrinks for these merchants, so every week of data makes the manual categorization burden worse.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-02** (P1, added 2026-07-16) - Do this session. Two legitimately distinct same-day charges collide on the natural key - silent data loss in a finance tool.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-04** (P1, added 2026-07-16) - Do this session. Hard-coupled to the escalated `data-integrity-02`: the item's own note says shipping either alone trades one bug for another. They must land in the same commit.
- **bar-raise-2026-07-17-yab-finance-product-categorization-tail-02** (P1, added 2026-07-17) - Do this session. Saving the entire normalized merchant string as an exact substring rule bakes per-transaction fragments into permanent rules - it makes tail-01 worse every time a user tries to fix it.

### Everything

- **bar-raise-2026-07-17-yae-yae-amber-token-means-three-colors** (P2, added 2026-07-17) - Do this session. One token name, three colours, three tiers - a rename and a value sync.
- **bar-raise-2026-07-17-yae-yae-archive-not-clean-terminal-store** (P2, added 2026-07-17) - Defer to after the queue enum validation lands. Now 471 rows with 108 duplicate ids, up from the 4 filed. Deduping an append-only audit store is a judgment call about which copy wins, and it should not be made before the schema is pinned.
- **bar-raise-2026-07-17-yae-yae-barraise-run-cost-unmeasured** (P1, added 2026-07-17) - Promote to scheduled task. Cost-per-closed-action is a number that has to be re-measured to mean anything - a one-off measurement goes stale the week after it is taken.
- **bar-raise-2026-07-17-yae-yae-budget-scheduler-off-template** (P2, added 2026-07-17) - Do this session. Partially closed already (head blocks landed in 14ff18a); finishing the body markup is the smaller remaining half.
- **bar-raise-2026-07-17-yae-yae-cp1252-json-readers-mojibake-live** (P1, added 2026-07-17) - Do this session. Reproduced again this run - reading the queue without `encoding='utf-8'` still raises UnicodeDecodeError. Twelve reads, one flag each.
- **bar-raise-2026-07-17-yae-yae-four-project-registries** (P2, added 2026-07-17) - Do this session. Four registries in three languages with nothing binding them; one assertion in the release check closes it.
- **bar-raise-2026-07-17-yae-yae-gate-payload-decode-guard-residual** (P2, added 2026-07-17) - Do this session. The gate guard asserts a regex and decodes nothing; given the two escalated Hordes gate items, a guard that actually decodes the payload is the thing that would have caught them.
- **bar-raise-2026-07-17-yae-yae-gitignore-no-secret-shape-patterns** (P2, added 2026-07-17) - Do this session. Nothing is exposed today, which is exactly when to add the patterns - paired with the `git add -A` item it is the same fifteen minutes.
- **bar-raise-2026-07-17-yae-yae-guards-absent-from-workflow-path** (P1, added 2026-07-17) - Do this session. Guards that only run on the low-traffic path are decoration; moving them into the workflow is the whole value of having written them.
- **bar-raise-2026-07-17-yae-yae-handover-roadmap-stale-scheduling** (P2, added 2026-07-17) - Do this session. A roadmap marked Active whose own table says all seven phases are done - doc-only.
- **bar-raise-2026-07-17-yae-yae-housekeeping-tracked-cruft** (P2, added 2026-07-17) - Do this session. 56 untracked loop probes are a plain `rm`; the 5 tracked queue snapshots are ~360KB of served cruft.
- **bar-raise-2026-07-17-yae-yae-local-git-never-packed** (P2, added 2026-07-17) - Do this session. 1.19 GiB of loose objects against a 58MB tree on a FUSE mount; `git gc` is one command and the FUSE disk is shared with everything else.
- **bar-raise-2026-07-17-yae-yae-project-context-stale-and-v1** (P2, added 2026-07-17) - Do this session. YaE is the only project still on schema v1 while twelve siblings are on v1.1 - straightforward catch-up.
- **bar-raise-2026-07-17-yae-yae-queue-get-returns-unfiltered-blob** (P2, added 2026-07-17) - Do this session. `queue-edit.ps1 get` emits the whole 1.1MB queue; a `list`/`filter` op is why this triage task has to hand-roll Python every night.
- **bar-raise-2026-07-17-yae-yae-queue-no-enum-validation** (P2, added 2026-07-17) - Do this session. This report had to normalise `P1`-vs-`1` and `high`-vs-`HIGH` to group 438 items - the fork is actively costing every reader.
- **bar-raise-2026-07-17-yae-yae-release-add-all-sweeps-operational-state** (P1, added 2026-07-17) - Do this session. Same root as the item above - `git add -A` against paths that were tracked before .gitignore named them. Fix them together.
- **bar-raise-2026-07-17-yae-yae-release-step-ordering-corrupt-json-ships** (P1, added 2026-07-17) - Do this session. Validate-then-write is backwards and the writer replaces the live file before it can throw - a one-block reorder in release.ps1.
- **bar-raise-2026-07-17-yae-yae-skill-artifacts-stale-vs-source** (P2, added 2026-07-17) - Do this session. Ten packaged skills older than their source - and two escalated Hordes items are specifically blocked on stale packaged skills, so this unblocks them.
- **bar-raise-2026-07-17-yae-yae-skillreview-tree-public** (P2, added 2026-07-17) - Do this session. 143 tracked files served publicly behind an advisory robots rule; same class as the tracked-queue item and should be decided with it.
- **bar-raise-2026-07-17-yae-yae-skipgate-cannot-fire** (P1, added 2026-07-17) - Do this session. The skip-gate is why bar-raise cost is what it is; a gate that cannot fire is the cost driver behind the sibling cost item.
- **bar-raise-2026-07-17-yae-yae-statusjson-contract-unversioned** (P2, added 2026-07-17) - Defer to the status-JSON writer consolidation. Adding schemaVersion means little until the writers agree on a shape - the Budget barRaise-stripping incident is the evidence that they do not.
- **bar-raise-2026-07-17-yae-yae-subpage-no-heading-structure** (P2, added 2026-07-17) - Do this session. Seven project pages with h1 only - the same generator writes them all, so it is one template edit.
- **bar-raise-2026-07-17-yae-yae-subpage-no-reduced-motion-focus-visible** (P2, added 2026-07-17) - Do this session. Eighteen pages with no visible focus ring; one shared CSS block covers all of them.
- **bar-raise-2026-07-17-yae-yae-tracked-queue-artifacts-public** (P1, added 2026-07-17) - Do this session. Re-confirmed live today: `https://yesandeverything.com/.work-queue.json` answers 200 with 1,103,202 bytes. Every finding in this report is public, including the ones describing open exposures.
- **bar-raise-2026-07-17-yae-yae-work-queue-runner-pwsh-not-installed** (P2, added 2026-07-17) - Do this session. Re-verified today: `pwsh` is still not on PATH and `C:\Program Files\PowerShell` does not exist. Four skill lines invoke a binary that has never been present.

### Ring

- **canonical-audit-2026-07-17-ring-scrubdash-emdash-guard** (P2, added 2026-07-17) - Do this session. A blind em-dash strip that silently breaks TICA parsing; latent, cheap, and it rides along with the Ring deploy batch that five escalated items are already waiting on.

### Cattery

- **working-tree-2026-07-17-cattery-stale-docs-tree** (P2, added 2026-07-17) - **closed by a concurrent routine mid-run**; graded "Do this session" at scan time, marked `completed` by another pass before this report was written. No action needed.

### Skylight

- **canonical-audit-2026-07-02-skylight-changelog-phantom** (P3, added 2026-07-16) - Do this session. Re-verified today: `changelog_path` still points at a `CHANGELOG.md` that has never existed. One-line choice, and it rides the same attended Skylight session as the fifteen escalated items.
- **bar-raise-2026-07-17-skylight-maintainability-01** (P1, added 2026-07-17) - Do this session. A timezone bug shipped and was fixed in this exact untested code within one month - and escalated `data-integrity-04` is another bug in the same file. Test it before touching it again.
- **bar-raise-2026-07-17-skylight-security-01** (P2, added 2026-07-17) - Do this session. An allowlist that fails open on an empty value, on the inbound path to a real family's calendar. Treat unset as reject-all.

### Gnosis

- **bar-raise-2026-07-16-gnosis-answers-full-store-walk** (P2, added 2026-07-16) - Defer to after the answers gate settles. The read path was just rewritten to filter secret answers; re-shaping it for caching now would land on top of a fresh change. Revisit once the new gate has run a week.
- **bar-raise-2026-07-16-gnosis-buildmjs-seven-esc-defs** (P2, added 2026-07-16) - Do this session. Re-verified today: still nine `esc()` definitions in a 6,337-line `build.mjs`, up from seven when filed. Three different escaping rules in one file is a latent correctness bug, not just tidiness.
- **bar-raise-2026-07-16-gnosis-kv-write-failures-swallowed** (P2, added 2026-07-16) - Do this session. Same shape as the Ring and Cattery observability items - structured logging on the failure branches.
- **bar-raise-2026-07-16-gnosis-release-gate-check-page-only** (P1, added 2026-07-16) - Do this session. Sharpened by this pass: the data hole this check failed to catch is now closed (see the escalated Gnosis item, completed), which makes the check itself the remaining gap. Assert the data path or the same class of hole ships green again.
- **bar-raise-2026-07-16-gnosis-uncapped-unauth-kv-writes** (P2, added 2026-07-16) - Do this session. Three unauthenticated POST paths with no length cap and no rate limit; the sibling path already has the guard to copy.
- **canonical-audit-2026-07-16-gnosis-catalog-ogl-provenance** (P3, added 2026-07-16) - Do this session. One provenance paragraph in PROJECT_SPEC; the attribution already exists on the page, the spec just does not record it.
- **canonical-audit-2026-07-16-gnosis-public-to-gated-links** (P2, added 2026-07-16) - Do this session. 71 in-prose links on 40 public pages 401 for every player, up from 5 on 3 - it is growing, and the build reports 'no broken links' while it does.
- **canonical-audit-2026-07-17-gnosis-character-gate-undocumented** (P1, added 2026-07-17) - Do this session. A second, weaker hiding tier documented only in a code comment, with `hidden_from` still absent from both PROJECT_SPEC and CLAUDE.md. Zero files use it today, which is precisely why writing it down now is cheap.
- **canonical-audit-2026-07-17-gnosis-milestone-stamp-hand-maintained** (P3, added 2026-07-17) - Do this session. It drifted twice in two days and went factually inverted - generate the counts or drop them.
- **canonical-audit-2026-07-20-gnosis-dramatis-undocumented** (P3, added 2026-07-20) - Do this session. One spec paragraph for a shipped DM-gated surface.

### Cross-project

- **working-tree-2026-07-17-queue-duplicate-stale-tree-items** (P2, added 2026-07-17) - Do this session. Parts 1 and 2 are queue hygiene this task can act on directly; part 3 (dedup by project+kind) is a small change to the scan and stops the duplicates recurring.

### Auto-applied this pass

**No items dropped.** Every candidate whose evidence looked like it might have gone stale was checked directly and every one still reproduced: `pwsh` is still absent from PATH and from `C:\Program Files\PowerShell`; the Rising hazard-catalog path `personal-skills-src/skills/code-audit/hazards/per-project/BR.md` still does not exist; Skylight's `CHANGELOG.md` still does not exist; HBH's `source/ui/main_menu/settings.gd` and `.tscn` still reference only each other; Gnosis `build.mjs` still defines nine `esc()`. Nothing in the aging set has an unambiguous drop case. The hourly drains have been re-verifying these items continuously, which is why there is no stale residue to clear.

Three status changes were applied to `.work-queue.json`:

- **`bar-raise-2026-07-16-gnosis-secret-answers-ungated-api`** -> `completed`. Evidence: Gnosis HEAD `3ddfdf2` gates the answers read path in `web/worker.js:319-329`, and the live unauthenticated GET returns 95,966 bytes / 182 records / 0 DM-tagged against 380,190 bytes last pass. Specifics deliberately still not restated here - this queue is served publicly.
- **`canonical-audit-2026-07-10-skylight-frame-id-empty-var`** -> `pending`. Evidence: `wrangler.jsonc:45` is `"SKYLIGHT_FRAME_ID": "4526946"`, a populated plain var, so the var-or-secret question the item was gated on is answered. The remaining work is doc-only: `README.md:71` and `src/index.ts:141` still instruct `wrangler secret put`.
- **`handler-audit-2026-07-17-skill-itself-stale`** -> `completed`. Evidence: the corrected source `SKILL.md` (2026-07-26, 17,101 bytes) names all twelve handler paths and carries neither stale path. The residual staleness is in the 2026-06-11 packaged `.skill`, which is a different item's scope.

**One concurrency note.** The queue was written by other routines twice during this pass. Each of the three changes above was applied on a fresh read with a byte-identical round-trip assertion and a post-write readback, so none of them clobbered a sibling write - but the counts in this report were taken at scan time and two items moved underneath it: `working-tree-2026-07-17-cattery-stale-docs-tree` was completed by another pass, and `canonical-audit-2026-07-28-cattery-commit-message-false-provenance` was newly escalated.
