# Queue triage (rolling)

Single living triage doc for `.work-queue.json`. Supersedes the dated
`QUEUE_TRIAGE-YYYY-MM-DD.md` snapshots and `QUEUE_PIPELINE_DIAGNOSIS-*.md`,
which were folded in here and removed; their point-in-time history remains in
git. The nightly queue-triage task overwrites the "Current state" section below
on each run, so this file always reflects the latest pass rather than spawning a
new dated file.

Last pass: 2026-07-26.
## Current state

**101 non-auto-safe items are aging past 7 days** (down from 109 last pass — six drained or completed, plus the two dropped last pass), alongside **55 items escalated to Kane** (up one). The worst offender by age in the aging set is `hbh-autopause-horde-day-signal-2026-07-14` at 12 days; in the escalated set the three completion gates (`yac-store-launch`, `yac-subscriptions-enable-ai`, `hbh-asset-production-pass`) hit **46 days**. Largest aging backlog is **Hordes at 33**, then **Everything at 25** and **Rising at 17**; largest escalated backlog is **Skylight at 15**.

**All 55 escalated items were re-verified against the repos, the live sites and live prod this pass, and all 55 still hold.** Nothing has self-healed. Both P0s reproduced: `https://gnosis.yesandeverything.com/api/answers` answered an unauthenticated GET with **200 and 380,188 bytes**, including entries tagged `"c":"DM"`; and Cattery prod (`emrerjdyujylgutfmool`) still reports `orders_cattery_id_fkey confdeltype='c'` with `0018`/`0019` present on disk but **still untracked in git and still unapplied**. The Hordes gate page still serves `var PASSWORD`, `var EDITOR` and `var ENCODED` to an unauthenticated GET (2,226,762 bytes).

**Twelve escalated Skylight items were reported for the first time this pass.** `bar-raise-2026-07-10-skylight-{dependency-01,dependency-02,observability-01,observability-02,observability-03,maintainability-02,architecture-01,reliability-03,data-integrity-02,data-integrity-03,data-integrity-04,solo-tool-ux-01}` were escalated by the 2026-07-25T03:37Z drain and did not appear individually in the previous pass. Every one was re-verified at its own file anchor this run and every one holds. They are the reason Skylight is the largest escalated slice.

**Four escalated items got sharper on re-verify.**
1. `bar-raise-2026-07-16-ring-verdict-enum-stale` is no longer latent. Ring's writer enum at `scripts/write-dashboard-status.ps1:96` is `('healthy','needs-attention','at-risk','stalled')`, but `status/data/Ring.json` currently holds `verdict: 'working'` — **a value its own writer will null on the next run**, exactly the way Apothecary was nulled. Apothecary has since been corrected to `('working','in-progress','needs-attention','stalled')` at `:198`; Ring and Cattery both still carry the old list.
2. `handler-audit-2026-07-17-skylight-audit-task-husk` — the evidence said the task last ran 2026-07-12 and had not run since. It has: `Get-ScheduledTask yac-skylight-audit` reports **LastRunTime 2026-07-19, LastTaskResult 1**. It is still Ready, still failing, and still undocumented in the handler. The double-schedule is live, not dormant.
3. `handler-audit-2026-07-17-skill-itself-stale` has narrowed to a packaging problem. The **source** at `_skill-review/personal-skills-src/skills/handler-audit/SKILL.md` now carries zero bogus `X:\` paths and names all twelve projects. The **shipped** copy does not: this session's own skill listing still describes handler-audit as covering "HBH, YaC, Scheduler, YaE". Source fixed, artifact stale.
4. `handler-audit-2026-07-10-gnosis-gate-password-decision` narrowed from five tracked files to three (`git grep -l` returns `CLAUDE.md`, `PROJECT_SPEC.md`, `web/worker.js:8`). The decision fork is unchanged.

**No item qualified for auto-application this pass.** Every aging item I could cheaply falsify was checked against the repo and held — including the whole Rising slice, re-checked because Rising landed a drift-fix commit (`d2e4867`) today. See Auto-applied.

### Escalated to Kane (blocked / blocked-on-user)

- **bar-raise-2026-07-24-hordes-gate-page-publish-01** (`blocked-on-user`, P1, added 2026-07-24) — re-verified by fetching `https://yesandeverything.com/hordes/`: HTTP 200, 2,226,762 bytes, body contains `var PASSWORD =`, `var EDITOR =` and `var ENCODED =`; `publish-gdd.ps1:303` still emits `var EDITOR = 'KanesChosen'`. **Unblocks when:** Kane decides whether `/hordes/` is a public page or a server-gated one — the fix branches entirely on that answer.
- **bar-raise-2026-07-24-hordes-security-03** (`blocked-on-user`, P1, added 2026-07-24) — same fetch, same literals; this is the security-lens framing of the item above and shares its fix. **Unblocks when:** the same public-vs-gated call lands; then the gate moves server-side in one Worker change.
- **bar-raise-2026-07-24-scheduler-gate-pages-v041-prose** (`blocked-on-user`, P2, added 2026-07-24) — re-verified in `projects/scheduler/index.html`: the meta description at `:7`, the og/twitter descriptions at `:12`/`:21` and the what's-new heading at `:238` all still say **v0.4.1**, while the live pill at `:218` reads **v0.7.1**. **Unblocks when:** Kane confirms the v0.7.x surface list (M7.1 org-switcher, per-seat entitlement, migrations 0009–0011) is what the public page should advertise.
- **bar-raise-2026-07-24-scheduler-posture-decision** (`blocked-on-user`, P2, added 2026-07-24) — no code state to verify; this is a one-question decision item and the question is unanswered. **Unblocks when:** Kane answers "driven to a first real user, or parked at v0.7.x" and it is written into `docs/DESIGN.md` §23.
- **canonical-audit-2026-07-20-cattery-projectspec-stale-sibling-name** (`blocked-on-user`, P3, added 2026-07-20) — re-verified: `PROJECT_SPEC.md` still names "Yes& Cats" / `cats.yesandeverything.com` at `:20`, `:23`, `:25` and in the locked-decisions line `:92`. **Unblocks when:** Kane confirms Yes& Cats was renamed to / superseded by Yes& Ring, which authorizes rewriting a locked line.
- **handler-audit-2026-07-17-skylight-audit-task-husk** (`blocked-on-user`, P2, added 2026-07-17) — re-verified via `Get-ScheduledTask`: `yac-skylight-audit` State=Ready, **LastRunTime 2026-07-19, LastTaskResult 1**. Still double-scheduled against the native `audit-skylight-nightly` routine, still failing, still absent from `CLAUDE.md:66-70`. **Unblocks when:** Kane says which of the two schedules is canonical — the Windows task gets deleted or the handler gets rewritten to document it, not both.
- **handler-audit-2026-07-17-skill-itself-stale** (`blocked-on-user`, P2, added 2026-07-17) — re-verified the source SKILL.md: bogus paths gone, all twelve projects named. The live skill listing still reads four projects, so the drift moved from source to artifact. **Unblocks when:** the `personal-skills` plugin is repackaged and reinstalled — the same repackage that closes Hordes `skill-suite-health-01`/`-02`.
- **bar-raise-2026-07-17-chains-compliance-02** (`blocked-on-user`, P1, added 2026-07-17) — re-verified: a case-insensitive grep of `legal/terms.md` for subscription, billing or refund returns **nothing**, while `src/subscription.ts:209` still ships Cancel-anytime copy. The app takes recurring payments against terms that never mention them. **Unblocks when:** Kane states the refund and cancellation policy in words — the clause cannot be drafted by inference.
- **bar-raise-2026-07-17-chains-compliance-03** (`blocked-on-user`, P1, added 2026-07-17) — re-verified: `_DRAFT_BANNER_HTML` still defined at `src/legal-view.ts:295` and injected into both the served Terms (`:316`) and Privacy (`:321`). **Unblocks when:** compliance-02 lands and Kane names an effective date; the banner should drop on a complete document, not an incomplete one.
- **bar-raise-2026-07-16-yab-dependency-02** (`blocked-on-user`, P1, added 2026-07-16) — re-verified live against GitHub this pass: `repos/TheProphetKane/yesandbudget/vulnerability-alerts` → **HTTP 404**, `automated-security-fixes` → `{"enabled":false,"paused":false}`. The doc half was already corrected in `a962350`; the toggle half is untouched. **Unblocks when:** Kane flips Dependabot alerts on in the repo settings UI — a repo-admin action no routine can perform.
- **bar-raise-2026-07-16-yab-data-integrity-02** (`blocked-on-user`, P1, added 2026-07-16) — re-verified both anchors: `apps/api/src/plaid/sync.ts:102` still issues a bare `ON CONFLICT(plaid_transaction_id) DO NOTHING` against the **partial** index created at `apps/api/src/db.ts:293` (`WHERE plaid_transaction_id IS NOT NULL`). Plaid sync still fails at PREPARE time. **Unblocks when:** an attended Budget session runs `scripts/release.ps1` — the one-line predicate fix is written but shipping is out of drain scope.
- **canonical-audit-2026-07-16-cattery-orders-cascade-prod-unapplied** (`blocked-on-user`, **P0**, added 2026-07-16) — re-verified against live prod this pass: `pg_constraint` still reports `orders_cattery_id_fkey confdeltype='c'`, and `git status` still shows `0018`/`0019` (plus `0009`/`0010`) as **untracked**. A breeder deleting their account still cascades away the escrow audit trail. **Unblocks when:** Kane approves DDL against live prod — applying `0018`+`0019` and committing the four untracked migrations is a write no unattended run may make.
- **bar-raise-2026-07-16-ring-verdict-enum-stale** (`blocked-on-user`, P3, added 2026-07-16) — re-verified and **sharpened**: `scripts/write-dashboard-status.ps1:96` lists `('healthy','needs-attention','at-risk','stalled')` while `status/data/Ring.json` holds `verdict: 'working'`, so the next status write nulls it. **Unblocks when:** an attended Ring session ships the one-line enum correction via `release.ps1` — the fix has no value unreleased.
- **bar-raise-2026-07-16-cattery-verdict-enum-stale** (`blocked-on-user`, P3, added 2026-07-16) — re-verified: same stale list at `scripts/write-dashboard-status.ps1:83`. Cattery.json currently holds `needs-attention`, which is in both enums, so it is not nulled today — but any `working`/`in-progress` verdict would be. **Unblocks when:** an attended Cattery session ships the same one-line enum correction.
- **bar-raise-2026-07-16-gnosis-secret-answers-ungated-api** (`blocked-on-user`, **P0**, added 2026-07-16) — re-verified by unauthenticated fetch: `https://gnosis.yesandeverything.com/api/answers` → **HTTP 200, 380,188 bytes**, first record tagged `"c":"DM"`. Gated DM content is served on an ungated data path, live right now. **Unblocks when:** Kane decides what the one public consumer of that endpoint should receive instead — the Worker gate cannot be written until that shape is chosen.
- **gnosis-release-62-proposed-dm-answers-2026-07-16** (`blocked-on-user`, P1, added 2026-07-16) — re-verified: `web/proposed-answers.json` holds 673 keys and **neither `44a4e485` nor `56972f9c` is present**, so the narrowed 4-key scope is still outstanding. **Unblocks when:** Kane gives an attended "go" for a Gnosis release — the routine may recover the answers but may not run the release that lands them.
- **canonical-audit-2026-07-12-ring-discord-role-mismatch** (`blocked-on-user`, P3, added 2026-07-12) — re-verified both anchors: `CLAUDE.md:79-80` says the category grants the **Cats** role, `PROJECT_SPEC.md:101-102` says **Ring**. Genuine one-word contradiction. **Unblocks when:** Kane looks at the live Discord category and says which role name is actually granted.
- **bar-raise-2026-07-10-yab-plaid-takeover-window** (`blocked-on-user`, P1, added 2026-07-10) — re-verified: `PlaidConnectFlow.tsx:90-93` still hard-codes the 90-day window with its own comment conceding Phase 2 should ask Plaid for `earliest_available_date`. **Unblocks when:** the account leaves the Plaid Sandbox — the double-count is latent until real 24-month backfill arrives, so this is event-gated rather than decision-gated.
- **handler-audit-2026-07-10-gnosis-gate-password-decision** (`blocked-on-user`, P1, added 2026-07-10) — re-verified: `git grep -l` finds the literal in **three** tracked files (`CLAUDE.md`, `PROJECT_SPEC.md`, `web/worker.js:8`), down from five. **Unblocks when:** Kane picks a fork — accept a committed low-stakes literal in a private repo and drop the `.secrets` framing, or scrub all three and rotate `GATE_PASSWORD` via wrangler.
- **handler-audit-2026-07-10-webhook-boilerplate** (`blocked-on-user`, P2, added 2026-07-10) — re-verified against `X:\.secrets\*\scripts\`: Hordes has 1 of 5 webhook files, Chains 1 of 5, Apothecary 3, Scheduler 3, Ring 4 (one `.revoked`), Budget 4, Rising 4, Gnosis 6 under its naming exception. Every handler still recites the same five-webhook block. **Unblocks when:** Kane either creates the missing Discord webhooks or authorizes trimming the block to what each project actually has.
- **scheduler-auditlog-composite-index-2026-07-10** (`blocked-on-user`, P3, added 2026-07-10) — re-verified: `migrations/0011_audit_log_composite_index.sql` exists on disk and is **untracked**, in a tree with seven other modified files. **Unblocks when:** an attended Scheduler session commits and applies 0011 — the decision is already resolved, only the apply is left.
- **working-tree-2026-07-10-ring-stale-tree** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `git status` in `X:\YesAndRing` still shows 8+ modified files including `src/index.js` and `public/app.js`. Still stranded human edits. **Unblocks when:** Kane says whether the in-flight edits are wanted — a routine cannot commit or discard someone else's uncommitted work.
- **canonical-audit-2026-07-10-cattery-photos-bucket-listing** (`blocked-on-user`, P1, added 2026-07-10) — re-verified against live prod: the `photos public read` storage policy is **still present** in `pg_policies`, and the repo-side fix `0018_drop_photos_public_listing.sql` is still untracked. **Unblocks when:** Kane approves the same prod-write session as the P0 above — both are one `supabase db push` apart.
- **canonical-audit-2026-07-10-skylight-frame-id-empty-var** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `wrangler.jsonc:42` still declares `"SKYLIGHT_FRAME_ID": ""` as an empty plain-text var while README and code say to set it as a secret. **Unblocks when:** Kane confirms the frame id is not sensitive (keep it a var, populate it) or is (delete the var, `wrangler secret put`).
- **canonical-audit-2026-07-10-gnosis-question-status-contract** (`blocked-on-user`, P3, added 2026-07-10) — re-verified: `PROJECT_SPEC.md:244` says questions "are NEVER closed" (locked 2026-07-06) while `web/build.mjs:256` defines `QSTATUS = { "?": "open", "x": "answered", "~": "lost" }` — two closed states. **Unblocks when:** Kane says which is canon; the spec line is locked, so the code cannot be changed to match by inference.
- **hbh-velocity-off-critical-path-mission-layer-2026-07-10** (`blocked-on-user`, P1, added 2026-07-10) — re-verified: `source/data/mission.gd` and `source/data/missions/` **still do not exist**, and `wave_director.gd:7` still declares only `all_waves_cleared`. The mission layer remains unbuilt. **Unblocks when:** Kane commits the next few HBH versions to the mission data layer instead of art wiring — a priority call, since the art track is itself gated on him.
- **bar-raise-2026-07-10-skylight-dependency-01** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/skylight.ts` still rides the unofficial reverse API and the repo contains **no test files and no vitest dependency**, so no contract probe exists. **Unblocks when:** Kane accepts a scheduled write-probe against the family's real Skylight account, or rules it out; every design here writes real data.
- **bar-raise-2026-07-10-skylight-observability-01** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/index.ts:102-103` returns a 500 from a bare catch and a grep for `console.error` across `src/` returns **zero hits**. **Unblocks when:** an attended Skylight session ships the log line — additive-only rule means it needs a human deploy, not a decision.
- **bar-raise-2026-07-10-skylight-observability-02** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `handleEmail` opens at `src/email.ts:29` but its first `try` is at `:56`, and `src/index.ts:109` runs the whole thing under `ctx.waitUntil`, so a parse failure leaves no trace. **Unblocks when:** the same attended Skylight deploy session runs; pairs with observability-01.
- **bar-raise-2026-07-10-skylight-observability-03** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/index.ts:80` still returns `/api/health` unconditionally, with the deep probes PIN-gated. **Unblocks when:** Kane picks the surface — fold a downstream check into `/api/health`, or point an external uptime probe at `/api/setup`.
- **bar-raise-2026-07-10-skylight-maintainability-02** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `esc()` still exists twice, at `src/ui.ts:128` (inside a JS template-literal string tsc never checks) and `:393` (TS). **Unblocks when:** an attended Skylight session extracts the client script; this is a refactor with a deploy, out of additive-only drain scope.
- **bar-raise-2026-07-10-skylight-architecture-01** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/router.ts` still hand-builds two payloads, `:47` for Google and `:54-57` for Skylight, and only the Skylight branch passes `categoryIds`. `src/types.ts` exists but carries no canonical event type. **Unblocks when:** an attended session lands the uniform `createEvent(env, CanonicalEvent)` signature — it changes both drivers at once and needs a live calendar test.
- **bar-raise-2026-07-10-skylight-reliability-03** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/google.ts` caches the token at `TOKEN_KEY = "g:token"` (`:21`) and calls `getAccessToken` at `:99`/`:138` with **no delete-and-retry on 401/403**, unlike the Skylight client. **Unblocks when:** the same attended deploy session runs; the retry cannot be validated without a real revoked-token case.
- **bar-raise-2026-07-10-skylight-data-integrity-02** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/google.ts:78` and `:131` both call `res.json()` after the write has committed, so an unreadable body throws on a successful write. **Unblocks when:** the same attended deploy session runs; pairs with data-integrity-01.
- **bar-raise-2026-07-10-skylight-data-integrity-03** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/email.ts:108-111` reads `Message-ID` **only** to set `In-Reply-To`/`References`; there is no dedup check anywhere. Email Routing is at-least-once. **Unblocks when:** an attended session adds the short-TTL KV seen-set; it needs a KV namespace decision Kane has not made.
- **bar-raise-2026-07-10-skylight-solo-tool-ux-01** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/index.ts:87` returns the bare `{ok:false,message:"unauthorized"}` 401 and `src/ui.ts` contains no 401 handling or redirect. **Unblocks when:** an attended Skylight session ships the redirect — a UI change to a tool a real family uses daily.
- **bar-raise-2026-07-10-skylight-dependency-02** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `package.json:20` still pins `"wrangler": "^3.60.0"`, the EOL v3 major, against a 2026-06-01 compatibility date. **Unblocks when:** Kane accepts a wrangler v4 deploy-toolchain bump on a private Worker his family depends on — a major-version change is not an additive one.
- **bar-raise-2026-07-10-skylight-data-integrity-04** (`blocked-on-user`, P2, added 2026-07-10) — re-verified: `src/dates.ts:33` `epochOf` still anchors naive values to UTC while parsing absolute forms as true instants, and `src/router.ts` compares them without normalising. **Unblocks when:** Kane chooses the policy — normalise both endpoints through `DEFAULT_TIMEZONE`, or reject a mixed pair outright.
- **bar-raise-2026-07-08-cattery-webhook-event-loss** (`blocked-on-user`, P1, added 2026-07-08) — re-verified and reproduced from the code: `src/index.js:320` records the event via `srv_event_seen` **before** any DB write (`:326-340`), and `:342` returns 200 unconditionally, so a failed `srv_mark_order_paid` is lost on Stripe retry. `MAX_BODY_BYTES = 10_240` at `:351` still applies to `/api/stripe/webhook` (`:349`). **Unblocks when:** Kane authorizes an attended Cattery money-path session — this rewrites the paid-order write path and needs a live Stripe test.
- **bar-raise-2026-07-08-cattery-money-path-logging** (`blocked-on-user`, P1, added 2026-07-08) — re-verified: `console.error` appears **nowhere** in `src/index.js`, so every Stripe and RPC failure branch is silent in Workers logs. **Unblocks when:** the same attended Cattery money-path session runs; it should land with the item above.
- **bar-raise-2026-07-08-cattery-stripe-idempotency-key** (`blocked-on-user`, P2, added 2026-07-08) — re-verified: a grep for `Idempotency-Key` across `src/index.js` returns **nothing**; transfer, refund and checkout POSTs all go out without one. **Unblocks when:** the same attended money-path session runs.
- **bar-raise-2026-07-08-cattery-ratelimit-key-bypass** (`blocked-on-user`, P2, added 2026-07-08) — re-verified: `src/index.js:417` still keys the limiter on `auth.slice(-40)` when a Bearer header is present, falling back to `cf-connecting-ip` only when it is absent, so a spoofed token escapes the throttle. **Unblocks when:** the same attended session runs; it is a one-line key change but ships with the money path.
- **bar-raise-2026-07-07-ring-worker-observability** (`blocked-on-user`, P1, added 2026-07-07) — re-verified: `wrangler.jsonc:11` sets `"observability": { "enabled": true }` with **no logpush and no tail_consumer**, and `/api/health` still surfaces no cron last-run status. **Unblocks when:** Kane picks the sink (Logpush vs a Tail Consumer Worker) — the instrumentation shape follows from that.
- **bar-raise-2026-07-07-ring-community-ratings-backup** (`blocked-on-user`, P1, added 2026-07-07) — re-verified: `handleJudgeRating` at `src/index.js:668` still writes KV only, and `supabase/migrations/` still tops out at **0007**, so the mirror migration was never written. **Unblocks when:** an attended Ring session runs migration + `db push` + `wrangler deploy` together — none of the three is in drain scope.
- **bar-raise-2026-07-07-ring-dualwrite-atomicity** (`blocked-on-user`, P1, added 2026-07-07) — re-verified: `pgStateRead`/`pgStateWrite` at `src/index.js:746`/`:756` are still called with a bare `try {} catch {}` at `:786`, so a half-succeeded dual-write diverges silently. **Unblocks when:** the same attended Ring deploy session runs; batch it with the two items above.
- **bar-raise-2026-07-07-ring-worker-freetier-budget** (`blocked-on-user`, P1, added 2026-07-07) — re-verified: the cron fan-out and typeahead fan-out are unchanged, and the tree is still dirty with in-flight edits to `src/index.js`, so editing would clobber. **Unblocks when:** the stale Ring tree is resolved (see `working-tree-2026-07-10-ring-stale-tree`) — this rewrite cannot start on top of someone else's uncommitted work.
- **bar-raise-2026-07-07-ring-gdpr-data-endpoints** (`blocked-on-user`, P1, added 2026-07-07) — re-verified: a grep for `/api/user/data` and `/api/user/export` across `src/index.js` returns **nothing**. A paid product storing emails and private judge notes has no export or deletion path. **Unblocks when:** Kane approves the retention policy the privacy page must state — the endpoints are mechanical, the policy is not.
- **bar-raise-2026-07-07-ring-seed-pii-fictionalize** (`blocked-on-user`, P2, added 2026-07-07) — re-verified: `public/data/seed.json` still carries real names at `:36-37` and a real email at `:82`, served to every visitor. **Unblocks when:** Kane says whether this is consented seed data (document the exception) or not (fictionalize per the portfolio rule).
- **chains-br-auth-01-2026-07-04** (`blocked-on-user`, P1, added 2026-07-04) — re-verified: `worker/src/lib.js:1750` still reads `body.userId` with only a shape check, and `:1043` documents that trust model in a comment. Anyone knowing an entitled UUID can spoof Pro. **Unblocks when:** an attended Chains session ships the JWT verification via `release.ps1` plus an end-to-end auth test against the live paid Worker.
- **bar-raise-2026-07-04-yab-bulk-delete-no-backup** (`blocked-on-user`, P1, added 2026-07-04) — re-verified line by line: `apps/api/src/server.ts:532-542` still goes straight from body parse to `DELETE FROM transactions` with **no `createBackup` call**, unlike every other mutating route. **Unblocks when:** an attended Budget session ships it via `scripts/release.ps1`.
- **canonical-audit-2026-07-02-skylight-google-phase2-framing** (`blocked-on-user`, P2, added 2026-07-02) — re-verified: `.project-context.json:79` still frames the Google Calendar driver as "phase 2" while it is shipped and primary in `wrangler.jsonc`, `router.ts` and the README. **Unblocks when:** an attended Skylight session rewrites the three stale spots; it is a canonical-doc write the drain is barred from.
- **hbh-multimesh-crowd-migration-2026-06-28** (`blocked-on-user`, P1, added 2026-06-28) — re-verified: `enemy_field_3d.gd:75-88` now reads `debug_flags.use_crowd3d` and `:446-451` implements the MultiMesh path with a per-Node fallback. The renderer is **wired but not defaulted**. **Unblocks when:** Kane accepts a frame profile at 2000/3000 enemies as sufficient evidence to flip the flag default to true.
- **yac-store-launch** (`blocked-on-user`, P1, added 2026-06-10, **46 days**) — completion gate, no code state to re-verify; Chains is still at `v0.58.3` with no store listing work in the tree. **Unblocks when:** Kane opens the Play Store developer account and commits to a TWA build date.
- **yac-subscriptions-enable-ai** (`blocked-on-user`, P1, added 2026-06-10, **46 days**) — re-verified: `src/caddy.ts:62` still reads `export const CHAINS_VISIBLE = false;`, so the AI caddy is dark. **Unblocks when:** subscription billing is live end to end — the flag flip is one line and is the *last* step, not the first.
- **hbh-asset-production-pass** (`blocked-on-user`, P1, added 2026-06-10, **46 days**) — completion gate; most art remains outstanding after the native-3D rework. **Unblocks when:** Kane queues the Midjourney batches per the personalization standard, or hands the gap list to Erik.

### Hordes

33 aging items — 32 from the 2026-07-17 bar-raise wave, one older. Nothing has drained here since that wave landed; the slice is now 121 queue items with **zero** marked `auto_safe`.

- **hbh-autopause-horde-day-signal-2026-07-14** (P1, added 2026-07-14) — Do this session. Oldest aging item in the portfolio at 12 days. Re-verified: `wave_director.gd:7` still declares only `all_waves_cleared`, so the `auto_pause.gd:88` `has_signal` guard fails closed and the subsystem is dead end to end.
- **bar-raise-2026-07-17-hordes-security-01** (P1, added 2026-07-17) — Do this session. Unvalidated KV values land in an `innerHTML` attribute sink at `GDD.html:6907`/`:8241`; inert only because the fetch 404s, which is an accident rather than a mitigation.
- **bar-raise-2026-07-17-hordes-security-02** (P2, added 2026-07-17) — Do this session. `cf-worker-canonical-state.js:89` skips the allowlist when `Origin` is absent — a public unauthenticated write. Pairs with security-01.
- **bar-raise-2026-07-17-hordes-incident-runbook-currency-01** (P1, added 2026-07-17) — Do this session. The v0.73.3 deploy never ran, so `/api/hbh-canonical-state` 404s; close both security items first, then deploy. One session covers all three.
- **bar-raise-2026-07-17-hordes-playability-02** (P1, added 2026-07-17) — Do this session. `_toggle_pause` (`world_3d.gd:906`) flips a local bool while `enemy_field_3d._physics_process` keeps pathing and damaging the CP. A player-visible bug, and the prerequisite for the escalated auto_pause item.
- **bar-raise-2026-07-17-hordes-per-project-ps1-stack-01** (P1, added 2026-07-17) — Do this session. Add the change-detected worker-deploy step to `release.ps1` (model on Chains `release.ps1:380-398`); this is the *cause* of the two-month-dead endpoint above.
- **bar-raise-2026-07-17-hordes-skill-chain-handoff-integrity-01** (P1, added 2026-07-17) — Do this session. The `(project, finding_id)` dedupe matched a *completed* item and silently swallowed HBH's only open HIGH for two runs; every future run stays corrupt until this is fixed.
- **bar-raise-2026-07-17-hordes-skill-suite-health-01** (P1, added 2026-07-17) — Do this session. Repackage and reinstall `personal-skills`. This is the same repackage that closes the escalated `handler-audit` skill item — they land together.
- **bar-raise-2026-07-17-hordes-skill-suite-health-02** (P1, added 2026-07-17) — Do this session. Repackage `code-audit` so the 2026-06-29 hazard catalog ships; rides the same pass.
- **bar-raise-2026-07-17-hordes-skill-trigger-collisions-01** (P2, added 2026-07-17) — Do this session. `[CHRONIC ×7]` — routing depends on the `anthropic-skills` copy shipping no description; replace the trigger phrase with a fully-qualified id. Also rides the repackage pass.
- **bar-raise-2026-07-17-hordes-solo-dev-voice-portfolio-01** (P1, added 2026-07-17) — Do this session. AI tool names in the public GDD changelog (Midjourney ×5, Claude ×4, a verbatim memory-key dump at v0.73.3) while `check_gdd_voice.py` passes green. Public surface, blind gate.
- **bar-raise-2026-07-17-hordes-maintainability-01** (P1, added 2026-07-17) — Defer to R3D-04. The dead 778-line research autoload versus the 5-tech stand-in is real, but which survives depends on the mission-layer decision that gates the escalated velocity item.
- **bar-raise-2026-07-17-hordes-reliability-02** (P1, added 2026-07-17) — Defer to R3D-04. The four orphaned `economy.gd` recorders have no call sites *because the mission layer does not exist*; wire-or-delete is premature.
- **bar-raise-2026-07-17-hordes-playability-01** (P2, added 2026-07-17) — Defer to R3D-04. `[CHRONIC ×7]` — this *is* the mission data layer, duplicating the escalated velocity item. Merge rather than carry twice.
- **bar-raise-2026-07-17-hordes-cost-economics-02** (P1, added 2026-07-17) — Promote to scheduled task. 30-day spend tripled to $779.87 while commits fell 210→68; per-routine cost attribution is standing measurement, not a one-off, and belongs in YaE's collector.
- **bar-raise-2026-07-17-hordes-reliability-03** (P2, added 2026-07-17) — Do this session. `_on_pause_menu`/`_on_pause_quit` discard a run with no save and no prompt while `options.gd:58` persists an `autosave_freq` nothing reads. A data-loss path with a settings toggle that lies.
- **bar-raise-2026-07-17-hordes-data-integrity-01** (P2, added 2026-07-17) — Do this session. Save/load erases the in-flight horde while restoring wave_director state, so a final-wave save fires a false `all_waves_cleared` on reload.
- **bar-raise-2026-07-17-hordes-save-format-stability-02** (P2, added 2026-07-17) — Do this session. `user://options.tres` lacks the temp+readback+rename write `world3d_save.json` already has, and `global.gd:11-15` has no null fallback across 75 unguarded call sites.
- **bar-raise-2026-07-17-hordes-playability-03** (P2, added 2026-07-17) — Do this session. Five of eight build hotkeys charge a different price than the menu shows; delete the `BUILDS` table and route through `BuildingCatalog3D`.
- **bar-raise-2026-07-17-hordes-compliance-data-steward-01** (P2, added 2026-07-17) — Do this session. Re-verified: neither `CREDITS.md` nor any `LICENSE` file exists, while the Godot logo (CC BY 4.0) is in the boot splash and Electrolize (OFL) is bundled. Attribution is an obligation.
- **bar-raise-2026-07-17-hordes-dependency-01** (P2, added 2026-07-17) — Do this session. Re-verified: `addons/Tripo3d_Godot_Bridge/` is present and enabled at `project.godot:43` with no license and no provenance row, falsifying `ASSET_PROVENANCE.md`.
- **bar-raise-2026-07-17-hordes-asset-pipeline-02** (P2, added 2026-07-17) — Do this session. `art-check.yml` filters only `assets/**` and `check_art.py` has no `.glb` branch, so 51 of 55 tracked `.glb` skip the documented silent-placeholder trap.
- **bar-raise-2026-07-17-hordes-incident-runbook-currency-02** (P2, added 2026-07-17) — Do this session. `cf-worker-setup.md` followed literally today orphans the live KV namespace holding real canonical state. A runbook that destroys data is worse than none.
- **bar-raise-2026-07-17-hordes-scheduled-task-firing-state-01** (P2, added 2026-07-17) — Do this session. Add the `write-dashboard-status.ps1` step to `audit-hordes-nightly`, mirroring `audit-apothecary-nightly:15`.
- **bar-raise-2026-07-17-hordes-audit-loop-integrity-01** (P2, added 2026-07-17) — Do this session. Record that HBH's canonical doc is release-gated, so the empty auto-fix lane reads as structural rather than as a failure every run.
- **bar-raise-2026-07-17-hordes-audit-loop-integrity-02** (P2, added 2026-07-17) — Promote to scheduled task. `write-dashboard-status.ps1:113/173` re-reads its own prior value, making 0/0/0 a permanent fixed point; the identical defect is filed against Rising as `observability-02`, so it is a portfolio-wide writer bug.
- **bar-raise-2026-07-17-hordes-maintainability-04** (P2, added 2026-07-17) — Do this session. Re-verified: `BEST_PRACTICES_PARITY.md:101` and `:178` still claim `world_3d.gd` went to "~800 lines" and `:105` still lists `unit_controller.gd` at 291, both measured higher. Small doc edit, numbers confirmed.
- **hbh-gdd-settings-backbone-stale-2026-07-17** (P2, added 2026-07-17) — Do this session. Re-verified: `GDD.html:1845` still points at `source/data/options.tres`, which **does not exist** (the live file is `source/data/options.gd`). Release-gated but trivial.
- **hbh-gdd-path-extends-phrasing-2026-07-17** (P3, added 2026-07-17) — Do this session. Re-verified: `GDD.html:4326` still names a path-extended subclass, a convention retired in v0.98.5. Bundle with the item above into one release.
- **hbh-orphan-mainmenu-settings-deadcode-2026-07-17** (P3, added 2026-07-17) — Do this session. Re-verified: `source/ui/main_menu/settings.gd` still exists, superseded by `source/ui/settings/`. Delete after the documented uid grep.
- **bar-raise-2026-07-17-hordes-scale-architecture-01** (P2, added 2026-07-17) — Defer to the perf pass. `_ring_cells` is O(R³) per unit per frame; it belongs with the escalated MultiMesh default-flip, since both are judged on the same profile run.
- **bar-raise-2026-07-17-hordes-voice-and-tone-01** (P2, added 2026-07-17) — Defer to the next naming pass. Six research nodes off the Order 42 frame; real but cosmetic, and cheaper batched with other content work.
- **bar-raise-2026-07-17-hordes-work-queue-hygiene-01** (P2, added 2026-07-17) — Do this session, re-filed with current numbers. Re-measured: the HBH slice is now **121 items (116 pending, 5 escalated), zero `auto_safe`** — the finding's own counts (9 items, 3 blocked P1s) are badly stale, but its claim that nothing drains automatically is more true than when filed, not less. Not a drop: correct the numbers rather than delete the finding.

### Everything

25 aging items, all from the 2026-07-17 bar-raise. This is the portfolio's own infrastructure, so each defect multiplies across every other project.

- **bar-raise-2026-07-17-yae-yae-tracked-queue-artifacts-public** (P1, added 2026-07-17) — Do this session. Re-verified live: `https://yesandeverything.com/.work-queue.json` returns **HTTP 200, 791,575 bytes** to an anonymous GET. Scope was corrected on 2026-07-26 — the queue and archive must stay tracked (git is their only backup), so this is a robots/serving fix, not a `git rm --cached` sweep.
- **bar-raise-2026-07-17-yae-yae-release-add-all-sweeps-operational-state** (P1, added 2026-07-17) — Do this session. `push-to-github.ps1:92` `git add -A` sweeps machine-churned files including a mid-write queue; it is the mechanism behind the item above. Fix them together.
- **bar-raise-2026-07-17-yae-yae-gitignore-no-secret-shape-patterns** (P2, added 2026-07-17) — Do this session. Re-verified: a case-insensitive grep of `.gitignore` for env/token/secret/webhook/pat/credential returns **zero** across all 56 lines, while `git add -A` stages everything. Third leg of the same tripod.
- **bar-raise-2026-07-17-yae-yae-cp1252-json-readers-mojibake-live** (P1, added 2026-07-17) — Do this session. Twelve `Get-Content` sites lack `-Encoding`, and the corruption is live. I hit this class of bug myself this run: reading the queue without `encoding=` raises `UnicodeDecodeError`, and printing repo content to a cp1252 stdout raises `UnicodeEncodeError`.
- **bar-raise-2026-07-17-yae-yae-release-step-ordering-corrupt-json-ships** (P1, added 2026-07-17) — Do this session. `release.ps1` validates status JSON at Step 1 *before* Step 2 writes it, and `write-dashboard-status.ps1:55` Move-Items over the live file before every post-write throw, so corrupt JSON ships with only a WARN.
- **bar-raise-2026-07-17-yae-yae-guards-absent-from-workflow-path** (P1, added 2026-07-17) — Do this session. Every integrity guard sits on `release.ps1`, which carries almost no traffic. Pairs with the item above: the ordering fix is worthless if nothing runs it.
- **bar-raise-2026-07-17-yae-yae-skipgate-cannot-fire** (P1, added 2026-07-17) — Do this session. The skip-gate cannot fire for three independent reasons and the previously-queued fix addresses one, so it would buy nothing. Re-scope before spending on it.
- **bar-raise-2026-07-17-yae-yae-barraise-run-cost-unmeasured** (P1, added 2026-07-17) — Promote to scheduled task. Measured floors of $35.59 and $44.54 in lens subagents alone against `actionsClosed 1` across nine runs. Cost-per-closed-action decides bar-raise cadence portfolio-wide and needs continuous tracking. Merges with Hordes `cost-economics-02`.
- **bar-raise-2026-07-17-yae-yae-local-git-never-packed** (P2, added 2026-07-17) — Do this session, and it worsens daily. Re-measured tonight: **24,522 loose objects** (23,234 when filed, 24,351 last pass), `in-pack 0`, `packs 0`, **1.22 GiB** against a 58MB working tree, plus 16 `tmp_obj_*` garbage files git now warns about by name. A `git gc` is one command.
- **bar-raise-2026-07-17-yae-yae-queue-no-enum-validation** (P2, added 2026-07-17) — Do this session. Confirmed again this run: `project` is spelled both `hordes` and `hbh`, both `chains` and `yac`, both `apothecary` and `yaa` in the live file, so every consumer normalizes by hand and `collect-usage.ps1:817` silently misattributes what it cannot match.
- **bar-raise-2026-07-17-yae-yae-archive-not-clean-terminal-store** (P2, added 2026-07-17) — Do this session, and re-measured: the archive now holds **500 rows with 108 duplicate ids** (the finding cited 4). The drain appends without an id check, so this grows every night it waits.
- **bar-raise-2026-07-17-yae-yae-statusjson-contract-unversioned** (P2, added 2026-07-17) — Do this session. Zero of the 13 files in `status/data/` carry a `schemaVersion`, and Skylight.json forks on both identity keys with nothing detecting it.
- **bar-raise-2026-07-17-yae-yae-four-project-registries** (P2, added 2026-07-17) — Do this session. Four hand-maintained project lists in three languages with no assertion binding them; the drift is silent by construction.
- **bar-raise-2026-07-17-yae-yae-project-context-stale-and-v1** (P2, added 2026-07-17) — Do this session. Re-verified: `.project-context.json:23` still has `version_pill_locations: []` and `:24` still has `release_script: null`, so the hub that defines the bar-raise contract is the only project failing it.
- **bar-raise-2026-07-17-yae-yae-queue-get-returns-unfiltered-blob** (P2, added 2026-07-17) — Do this session. Re-verified: `queue-edit.ps1:23` is still `ValidateSet('get','add','set','drop')` with no list/filter/summary op, so "what is pending for YaE" means dumping the whole file. I did exactly that this run.
- **bar-raise-2026-07-17-yae-yae-work-queue-runner-pwsh-not-installed** (P2, added 2026-07-17) — Do this session. The skill leads every command with `pwsh`, which is not installed on this host. A skill whose first line fails is a skill that does not exist.
- **bar-raise-2026-07-17-yae-yae-skill-artifacts-stale-vs-source** (P2, added 2026-07-17) — Do this session. 10 of 19 packaged `.skill` artifacts are older than their source. Now demonstrated rather than inferred: the handler-audit source names twelve projects while the loaded skill still names four (see the escalated item).
- **bar-raise-2026-07-17-yae-yae-skillreview-tree-public** (P2, added 2026-07-17) — Do this session. 143 tracked files under `_skill-review/` serve 200; `robots.txt` is advisory and blocks nothing from a normal user agent.
- **bar-raise-2026-07-17-yae-yae-amber-token-means-three-colors** (P2, added 2026-07-17) — Do this session. Re-verified: `index.html:63` `--amber: #cc8400`, `status/index.html:28` `--amber: #f5b860`, and the sub-pages `#cc5500` — one token name, three colours across three tiers.
- **bar-raise-2026-07-17-yae-yae-subpage-no-reduced-motion-focus-visible** (P2, added 2026-07-17) — Do this session. `:focus-visible` appears 7× in `index.html` and zero times on the other 20 pages; keyboard users cannot see focus anywhere but the homepage.
- **bar-raise-2026-07-17-yae-yae-subpage-no-heading-structure** (P2, added 2026-07-17) — Do this session. Seven project pages are `h1=1, h2=0, h3=0` and `404.html` has no `h1` at all, so screen-reader users get no navigable structure. Pairs with the item above as one accessibility pass.
- **bar-raise-2026-07-17-yae-yae-budget-scheduler-off-template** (P2, added 2026-07-17) — Do this session, scope already narrowed. The head block was brought onto the shared template in `14ff18a`; the body markup half is untouched. Relevant now because the escalated Scheduler prose item edits the same file — do both in one pass.
- **bar-raise-2026-07-17-yae-yae-gate-payload-decode-guard-residual** (P2, added 2026-07-17) — Do this session. `push-to-github.ps1:53-61` asserts a regex and nothing about the payload, with no brackish-rising equivalent at all. Directly relevant to the two escalated Hordes gate-page items.
- **bar-raise-2026-07-17-yae-yae-handover-roadmap-stale-scheduling** (P2, added 2026-07-17) — Do this session. `BAR_RAISE_ROADMAP.md:3` says Active while its own table marks all seven phases DONE, and `BAR_RAISE_HANDOVER.md:142` references a `scripts/schedule/` that is absent from disk and git.
- **bar-raise-2026-07-17-yae-yae-housekeeping-tracked-cruft** (P2, added 2026-07-17) — Do this session. 56 untracked loop probes (plain `rm`), 5 tracked queue snapshots needing `git rm --cached`, 4 tracked dated reports predating retention, and 7 unreferenced scripts. Note the corrected scope: the queue and archive themselves must stay tracked.

### Rising

17 aging items, all from the 2026-07-17 bar-raise. Re-checked in full this pass because Rising landed a drift-fix commit (`d2e4867`) today; **none of the seventeen was closed by it.**

- **bar-raise-2026-07-17-rising-solo-tool-ux-01** (P2, added 2026-07-17) — Do this session. Re-verified: `scripts/push-to-github.ps1` and `scripts/publish-gdd.ps1` still contain **no `2>&1` guard**, while `write-dashboard-status.ps1:237` has one. This is the root of the recurring release stderr-abort (memory `br-release-stderr-wrap`), so it unblocks every other Rising fix that has to ship.
- **bar-raise-2026-07-17-rising-architecture-01** (P2, added 2026-07-17) — Do this session. Re-verified: `scripts/check_best_practices.py:23` still pins `GOD_FILES` to a hardcoded list, so the four current >1000-line files are invisible to the ratchet.
- **bar-raise-2026-07-17-rising-maintainability-03** (P2, added 2026-07-17) — Do this session, scope narrowed. Re-verified: `ASSET_SCALE_MASTER` is still read at `scripts/check_playability.py:43` and still documented in `docs/RENDER_SCALING.md` and `docs/ARCHITECTURE.md` — but **no longer in `docs/SPINE-INTEGRATION.md`**, so the finding is two docs, not three. It contradicts the locked native-1.0x decision (memory `br-no-scale-constants`).
- **bar-raise-2026-07-17-rising-compliance-data-steward-01** (P2, added 2026-07-17) — Do this session. Re-verified: `docs/LICENSES.md` still does not exist, while the tree carries ~5.4 MB of committed spine-godot DLLs and Navy's commissioned art with no recorded terms.
- **bar-raise-2026-07-17-rising-orchestration-03** (P2, added 2026-07-17) — Do this session. Re-verified precisely: `.project-context.json:63` points `hazard_catalog` at the **relative** path `personal-skills-src/skills/code-audit/hazards/per-project/BR.md`; `X:\BrackishRising\personal-skills-src\` does not exist, so it does not resolve from the repo root. (A file of that name does exist under YaE's `_skill-review/`, which is what the field should point at if it is kept.)
- **bar-raise-2026-07-17-rising-game-design-02** (P2, added 2026-07-17) — Do this session, anchors moved. Re-verified: the grid-size mismatch is now handled at `source/autoloads/save_load.gd:461-477` (was cited at `:388-398`) and still only `push_warning()`s before restoring anyway.
- **bar-raise-2026-07-17-rising-data-integrity-03** (P2, added 2026-07-17) — Do this session. The tile-restore path still instantiates without an `is_in_bounds` check; pairs with game-design-02 as one save-hardening patch.
- **bar-raise-2026-07-17-rising-data-integrity-02** (P2, added 2026-07-17) — Do this session, scope narrowed. Re-verified: `tests/run_tests.gd` **does** carry `_test_save_is_crash_safe()`, which proves the atomic-write and backup-preservation contract — but there is still no state round-trip (seed→save→clear→load→assert) and no corrupt-JSON negative case. Write the two missing halves, not the whole suite.
- **bar-raise-2026-07-17-rising-observability-02** (P2, added 2026-07-17) — Do this session. Re-verified: `scripts/write-dashboard-status.ps1:75/169` still reuses `$existingFindings` or falls back to `high=0; medium=0; low=0` and never parses the audit report. Same defect as Hordes `audit-loop-integrity-02` — fix the shared shape once.
- **bar-raise-2026-07-17-rising-performance-04** (P2, added 2026-07-17) — Do this session, mechanism refined. Re-verified: `_find_nearest_target` is now at `source/enemies/enemy_base.gd:780-825` and pulls `global.get_cached_nodes_in_group("buildings"/"units")` — a frame-memoized helper has been added since the finding — but it still walks **both full arrays per enemy per acquisition** rather than using `spatial_index.get_units_near`/`get_buildings_near`. The lever is unchanged; the cited line range is not.
- **bar-raise-2026-07-17-rising-performance-01** (P2, added 2026-07-17) — Do this session. Re-verified: `docs/OPTIMIZATION_LOG.md:10` still reads that the native-art world is unmeasured as of v0.59.3. This is the baseline every later performance claim needs, including performance-04 above.
- **bar-raise-2026-07-17-rising-reliability-02** (P2, added 2026-07-17) — Do this session. `slide_move`, `apply_safe_push` and `push_edge_ok` carry contracts that regressed against each other twice (memory `br-unit-building-collision`) with no test guarding any of it.
- **bar-raise-2026-07-17-rising-maintainability-01** (P2, added 2026-07-17) — Do this session. Re-verified: `docs/GODOT_BEST_PRACTICES_ADHERENCE.md` still narrates the god-files at pre-decomposition sizes in the present tense while all six are now under 1000 lines.
- **bar-raise-2026-07-17-rising-maintainability-02** (P2, added 2026-07-17) — Do this session. `docs/ARCHITECTURE.md` still predates the v0.59.32/33 wave-2 extractions and carries stale line counts plus a Scout base contradiction. Bundle with maintainability-01 and -03 as one docs release.
- **bar-raise-2026-07-17-rising-compliance-data-steward-03** (P2, added 2026-07-17) — Do this session. Re-verified: `.project-context.json:89-90` still scopes the `no_hbh_cross_contamination` check to "BR source **or GDD**", so it keeps hitting the legitimate fork-history prose in `docs/GDD.html`. A gate that always fails never blocks.
- **bar-raise-2026-07-17-rising-game-design-03** (P2, added 2026-07-17) — Do this session, but **narrow it before doing it**. The `source/units/unit.gd:37-38` header genuinely describes a superseded scaling model and should be corrected. The second half — sweeping "inherited HBH version stamps" from `source/` — collides with memory `br-inherited-hbh-version-stamps`, which records the v0.60–v0.65 stamps across 58 files as deliberate fork provenance that must **not** be mass-rewritten. Re-verified that `save_load.gd:106/390/393/461` carries exactly those stamps. Do the header, drop the sweep.
- **bar-raise-2026-07-17-rising-public-voice-01** (P2, added 2026-07-17) — Do this session, in a YaE-scoped session. Two em-dash entities on the YaE-side brackish-rising gate page; the fix lands in `X:\YesAndEverything`, not BR, and the standing scope rule (memory `stay-in-assigned-project-scope`) makes it its own session rather than a drive-by.

### Gnosis

9 aging items. Two are the read-only halves of the escalated P0 exposure and should be sequenced behind it.

- **canonical-audit-2026-07-16-gnosis-public-to-gated-links** (P2, added 2026-07-16) — Do this session, and it grows fast. Public-to-gated in-prose links went **5→71 across 3→40 public pages in one day**; every one 401s for a player. Fix the build check, not the links — at that growth rate hand-fixing loses.
- **bar-raise-2026-07-16-gnosis-release-gate-check-page-only** (P1, added 2026-07-16) — Do this session, alongside the escalated `/api/answers` P0. The release check asserts the page gate and never the data path behind it, which is precisely why the ungated API survived a release.
- **bar-raise-2026-07-16-gnosis-answers-full-store-walk** (P2, added 2026-07-16) — Do this session, with the item above. `loadAnswers()` at `web/worker.js:135` cursor-walks the whole `a:` prefix on every public board load — the same code path the P0 exposes.
- **bar-raise-2026-07-16-gnosis-uncapped-unauth-kv-writes** (P2, added 2026-07-16) — Do this session. Unauthenticated write paths with no length cap, no rate limit, and acceptance of the privileged author name. Same Worker deploy as the two above.
- **bar-raise-2026-07-16-gnosis-kv-write-failures-swallowed** (P2, added 2026-07-16) — Do this session. The Worker swallows KV write failures and asserts status codes rather than payloads, so a silent write loss reads as success.
- **bar-raise-2026-07-16-gnosis-buildmjs-seven-esc-defs** (P2, added 2026-07-16) — Do this session, and it worsens. Re-verified: `web/build.mjs` now carries **8 `function esc` definitions** with differing escaping rules (7 when filed, 9 by the 07-24 recount) in a file that grew 5,332→6,337 lines. Divergent HTML escaping is an XSS shape, not just duplication.
- **canonical-audit-2026-07-17-gnosis-character-gate-undocumented** (P1, added 2026-07-17) — Do this session. The soft/hard viewing model lives only in a code comment and the spec never says `hidden_from` is not a gate. A doc gap now, expensive after the first `hidden_from` use.
- **canonical-audit-2026-07-16-gnosis-catalog-ogl-provenance** (P3, added 2026-07-16) — Do this session. `PROJECT_SPEC.md` still records no open5e/OGL/SRD provenance for the item catalog. Cheap, and it is a licensing record.
- **canonical-audit-2026-07-17-gnosis-milestone-stamp-hand-maintained** (P3, added 2026-07-17) — Promote to scheduled task. The §5 milestone stamp drifted twice in two days and went factually inverted; the digits were patched, but hand-maintained counts will drift again. Derive them or stop publishing them.

### Budget

5 aging items. The three finance-product items are one connected data-integrity story and should be worked as a set.

- **bar-raise-2026-07-16-yab-finance-product-dedupe-02** (P1, added 2026-07-16) — Do this session. The natural key `UNIQUE(account_id, posted_date, amount, raw_description)` at `db.ts:120` has no occurrence ordinal, so two legitimately distinct same-day charges collide and the CSV path counts the second as a duplicate and drops it. Silent data loss.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-04** (P1, added 2026-07-16) — Defer to the escalated `data-integrity-02` fix. This one only bites *after* the `ON CONFLICT` predicate is corrected, so it must ship in the same release or it converts one broken path into another.
- **bar-raise-2026-07-16-yab-finance-product-categorization-tail-01** (P1, added 2026-07-16) — Do this session. `[CHRONIC ×7]` — `merchant.ts:14` strips `/\*+\d+/g`, which requires digits immediately after the asterisk, so alphanumeric order tokens survive and those merchants normalize to a unique string per transaction. The tail can never shrink for them.
- **bar-raise-2026-07-17-yab-finance-product-categorization-tail-02** (P1, added 2026-07-17) — Do this session, with tail-01. The Categorize page saves a rule whose pattern is the entire normalized merchant string, so any surviving per-transaction fragment produces a rule that can never fire again. The fix pattern already exists in-repo at `Transactions.tsx:275-291`.
- **bar-raise-2026-07-16-cross-budget-openfindings-backfill** (P2, added 2026-07-16) — Do this session. Parts 1 and 2 shipped; this is the historical backfill of 63 of 86 `Budget.json` openFindings that exist only as dashboard entries with no queue item behind them. Until it runs, the Budget dashboard count is fiction.

### Chains

4 aging items, all money-path. All four need the live paid Worker, so they batch into one attended session with the two escalated compliance items.

- **bar-raise-2026-07-17-chains-dataint-01** (P1, added 2026-07-17) — Do this session. `worker/src/handlers/billing.js:640-693` handles only the happy path: no `charge.refunded` branch, no dispute branch, no reconciliation sweep. One dropped webhook strands a paying user or leaves a refunded one entitled indefinitely.
- **bar-raise-2026-07-17-chains-finance-02** (P2, added 2026-07-17) — Do this session, decision first. Neither `_checkSubscriptionEntitled` nor `is_entitled()` has a `past_due` branch, so a failed renewal is undefined behaviour. Kane picks the grace policy, then it is a small patch.
- **bar-raise-2026-07-17-chains-cost-01** (P2, added 2026-07-17) — Do this session. `handleExtractPhotosReal` and `handleParseCourseText` bypass `DAILY_AI_SPEND_CAP_USD` entirely and `photos[]` has no length bound. Latent only while `CHAINS_VISIBLE` is false — and flipping that flag is an escalated completion gate, so this must land *before* it.
- **bar-raise-2026-07-17-chains-dataint-02** (P2, added 2026-07-17) — Do this session, with cost-01. The spend counters are a KV read-modify-write, so caps overshoot under concurrency. Structural, not tuning: KV has no compare-and-swap. Same pre-condition on the `CHAINS_VISIBLE` flip.

### Skylight

3 aging items, on top of the 15 escalated. Skylight's tree is clean, which makes it the cheapest project to actually finish.

- **canonical-audit-2026-07-02-skylight-changelog-phantom** (P3, added 2026-07-16) — Do this session. Re-verified: `.project-context.json` still sets `changelog_path` to `CHANGELOG.md` and **no `CHANGELOG.md` exists** in the repo root. Either create it or null the field.
- **bar-raise-2026-07-17-skylight-maintainability-01** (P1, added 2026-07-17) — Do this session. Re-verified: there are **no test files and no vitest dependency** anywhere in the repo, while `src/dates.ts` and `src/parse.ts` are pure, fragile, and shipped a timezone bug that was fixed inside one month. It is also the prerequisite that makes the 12 escalated Skylight fixes safe to land.
- **bar-raise-2026-07-17-skylight-security-01** (P2, added 2026-07-17) — Do this session. Re-verified: `src/email.ts:47-50` still enforces `ALLOWED_SENDERS` only when non-empty, so an unset value would let any sender write to the family calendar. Treat empty as reject-all; the populated default means zero friction.

### Apothecary

2 aging items. Both are doc-accuracy, and the tree already carries an uncommitted partial fix.

- **bar-raise-2026-07-17-apothecary-discord-notify-header-stale** (P1, added 2026-07-17) — Do this session. Re-verified: `scripts/discord-notify.ps1:6` still tells the reader the webhook files live in-repo while `:39-40` read them from `X:\.secrets\YesAndApothecary\scripts\`, and `:15`/`:96` still use "Apothecary Push" and "YesAndApothecary Headline Bot" against the names locked in `CLAUDE.md:147`. A header that lies about its own code.
- **bar-raise-2026-07-17-apothecary-comment-accuracy-sweep** (P3, added 2026-07-17) — Do this session, half already done. Re-verified: `src/main.js:75` now reads `(v1.1.0)` — correct, but **uncommitted** (the tree shows ` M src/main.js`). The second half still holds: `CLAUDE.md`'s file-layout table lists every `src/util/` module except `migrate.js`. Commit the first half, write the second.

### Ring

1 aging item, on top of 8 escalated.

- **canonical-audit-2026-07-17-ring-scrubdash-emdash-guard** (P2, added 2026-07-17) — Do this session, but sequence it behind the stale tree. `scrubDash` in `src/index.js` risks a blind em-dash strip that would silently break TICA parsing, and the finding notes four prior audits recorded the false claim that all the em-dashes were in comments — 11 are not. `src/index.js` is one of the files sitting modified in the stale Ring tree, so this cannot be touched until that escalated item resolves.

### Cattery

1 aging item, on top of 8 escalated.

- **working-tree-2026-07-17-cattery-stale-docs-tree** (P2, added 2026-07-17) — Do this session. Re-verified: the tree still shows ` M PROJECT_SPEC.md`, ` M docs/DATA-MODEL.md`, ` M test/money.test.js` plus **four untracked migrations** (`0009`, `0010`, `0018`, `0019`). This item absorbed the untracked-migrations item on 2026-07-26 and is now the single item covering the whole tree — which means it is also the repo-side half of the escalated P0. Work it in the same attended session.

### Cross-project

1 aging item.

- **working-tree-2026-07-17-queue-duplicate-stale-tree-items** (P2, added 2026-07-17) — Do this session, scope already down to one third. Re-verified via the 2026-07-26T00:35Z drain note and confirmed against the live queue: three of the four duplicate ids are gone, both duplicate pairs have collapsed, and only `working-tree-2026-07-10-ring-stale-tree` survives — which is itself escalated. What remains is part 2 of the original finding: the two scanners still use different id conventions (`working-tree-*` vs `wq-*`) and will re-duplicate on the next scan. Fix the convention, not the leftovers.

### Auto-applied this pass

**Nothing.** No item met the bar for unattended removal.

The Drop bar is unambiguous falsified evidence — a referenced file that truly does not exist, a claim the repo directly contradicts. Every candidate was checked and every one held:

- The entire **Rising** slice (17 items) was re-verified from scratch because Rising committed a drift-fix pass (`d2e4867`) today. `docs/LICENSES.md` is still absent, `ASSET_SCALE_MASTER` is still read at `check_playability.py:43`, `GOD_FILES` is still hardcoded at `check_best_practices.py:23`, neither `push-to-github.ps1` nor `publish-gdd.ps1` has the `2>&1` guard, and the `hazard_catalog` path still does not resolve from the BR repo root. Three items narrowed (see the Rising section); none was falsified.
- **bar-raise-2026-07-17-hordes-work-queue-hygiene-01** was the strongest drop candidate: its literal counts (9 items, 3 blocked P1s) are false — the slice is now 121 items with zero `auto_safe`. But its claim is *more* true at the new numbers, so this is a re-file, not a delete. Deleting it would remove a true finding on the strength of a stale integer.
- **bar-raise-2026-07-17-apothecary-comment-accuracy-sweep** is half-fixed (`src/main.js:75` now reads `v1.1.0`) but the fix is uncommitted and the `CLAUDE.md` half is untouched, so the item still has work behind it.
- **handler-audit-2026-07-17-skill-itself-stale** looked closed at the source (all twelve projects named, bogus paths gone) but the shipped artifact is still the four-project version, proven by this session's own skill listing. Marking it completed would have recorded a fix that has not reached the point of use.

No escalated item was found unblocked, so no status was reverted to `pending` and nothing was marked `completed`. `.work-queue.json` is unmodified by this pass.
