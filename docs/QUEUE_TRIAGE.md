# Queue triage (rolling)

Single living triage doc for `.work-queue.json`. Supersedes the dated
`QUEUE_TRIAGE-YYYY-MM-DD.md` snapshots and `QUEUE_PIPELINE_DIAGNOSIS-*.md`,
which were folded in here and removed; their point-in-time history remains in
git. The nightly queue-triage task overwrites the "Current state" section below
on each run, so this file always reflects the latest pass rather than spawning a
new dated file.

Last pass: 2026-07-25.
## Current state

**109 non-auto-safe items are aging past 7 days** (up from 74 last pass — the whole 2026-07-17 bar-raise wave crossed the window at once), plus **54 items escalated to Kane**. The worst offender by age is `canonical-audit-2026-07-12-ring-discord-role-mismatch` at 13 days in the aging set, and the three 45-day completion gates (`yac-store-launch`, `yac-subscriptions-enable-ai`, `hbh-asset-production-pass`) in the escalated set. Largest aging backlog is **Hordes at 34**, then **Everything at 28** and **Rising at 19**; largest escalated backlog is **Skylight at 15**.

**Every one of the 54 escalated items was re-verified against the repos this pass, and every one still holds.** Nothing has silently self-healed. The two P0s are both confirmed live: `/api/answers` on gnosis.yesandeverything.com answered an unauthenticated GET with **200 and 380 KB** of the whole store this pass, and Cattery prod still reports `orders_cattery_id_fkey confdeltype='c'` with migrations `0018`/`0019` authored on disk but **untracked in git and unapplied to prod**. Two escalated items have narrowed rather than closed: HBH's MultiMesh crowd renderer is now wired behind `debug_flags.use_crowd3d` (default `false`), so it is no longer "unwired" — only un-defaulted; and the `handler-audit` skill source has had its two bogus paths corrected, leaving only the 6-of-12 coverage gap.

**One escalated finding got sharper on re-verify.** `bar-raise-2026-07-16-yab-data-integrity-02` was reproduced from first principles this pass rather than taken on the prior note: `apps/api/src/plaid/sync.ts:102` issues a bare `ON CONFLICT(plaid_transaction_id)` against the **partial** unique index created at `apps/api/src/db.ts:293` (`WHERE plaid_transaction_id IS NOT NULL`). SQLite rejects that pairing at PREPARE time — reproduced live: `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`. Adding the matching `WHERE` predicate to the conflict target prepares clean. Plaid sync is broken end to end, not degraded.

**Four aging items were verified fixed and removed** (see Auto-applied). Everything else in the aging set is live work that needs Nick's hand: the bulk of it is release-gated, decision-gated, or requires a deploy the unattended drain is barred from.

### Escalated to Kane (blocked / blocked-on-user)

- **bar-raise-2026-07-24-hordes-gate-page-publish-01** (`blocked-on-user`, PP1, added 2026-07-24) — re-verified by fetching the live page: `https://yesandeverything.com/hordes/` still answers an unauthenticated GET with `var PASSWORD = 'SneakPeak'`, `var EDITOR = 'KanesChosen'` and a plain-`atob` payload, emitted from `scripts/publish-gdd.ps1`. **Unblocks when:** Kane decides whether `/hordes/` is a public page or a real gate — if gated, the payload moves behind a server-side check; if public, the password literals come out and the framing goes.
- **bar-raise-2026-07-24-hordes-security-03** (`blocked-on-user`, PP1, added 2026-07-24) — same live fetch; same two literals still ship in a page committed to a public repo. **Unblocks when:** Kane authorises the server-side gate move (this is the code half of the decision above; one attended session closes both).
- **bar-raise-2026-07-24-scheduler-gate-pages-v041-prose** (`blocked-on-user`, PP2, added 2026-07-24) — re-verified `projects/scheduler/index.html:7`, which still reads "a multi-tenant employee-scheduling SaaS at **v0.4.1**" while Scheduler's HEAD is v0.7.1 with migrations through 0011. **Unblocks when:** Kane answers the posture question below — the rewrite's content depends on whether v0.7.x is the shipping story or a parking note.
- **bar-raise-2026-07-24-scheduler-posture-decision** (`blocked-on-user`, PP2, added 2026-07-24) — re-verified `X:\YesAndScheduler\docs\DESIGN.md` §23 (line 617): the Open Questions list has entries for usernames, notifications, unavailability, time zones, sub-dept deletion, billing model and auth — and **no posture entry**. **Unblocks when:** Kane says "driven to a first user" or "parked at v0.7.x" and it is written into §23 as a locked decision.
- **canonical-audit-2026-07-20-cattery-projectspec-stale-sibling-name** (`blocked-on-user`, PP3, added 2026-07-20) — re-verified `X:\YesAndCattery\PROJECT_SPEC.md`: lines 20, 23, 25 and the locked line 92 all still name "Yes& Cats" / `cats.yesandeverything.com`. **Unblocks when:** Kane approves editing a line marked *locked* (2026-06-29) — the edit itself is one find-and-replace to "Yes& Ring" / `ring.yesandeverything.com`.
- **handler-audit-2026-07-17-skylight-audit-task-husk** (`blocked-on-user`, PP2, added 2026-07-17) — re-verified via `Get-ScheduledTask`: `yac-skylight-audit` is still `State=Ready`, and it has run *since* the evidence was written — `LastRunTime=2026-07-19 06:30`, `LastTaskResult=1`. So it is not dormant, it is **actively failing weekly** while `CLAUDE.md:66-70` calls `scripts/audit.ps1` a manual escape hatch. **Unblocks when:** Kane says whether the native routine or the Windows task is the real audit — the loser gets unregistered, the handler follows.
- **handler-audit-2026-07-17-skill-itself-stale** (`blocked-on-user`, PP2, added 2026-07-17) — **partially resolved.** Re-read the source `SKILL.md`: both bogus paths are gone (it now names `X:\HereBeHordes` and `X:\YesAndScheduler`, and covers six handlers). Two gaps survive: the portfolio is twelve handlers, and the *installed* skills still advertise four (`personal-skills:handler-audit` still says "HTBH, YaC, Scheduler, YaE"), so the source fix has never been packaged. **Unblocks when:** Kane runs a repackage-and-reinstall of `personal-skills` — the coverage widening can ride along in the same pass.
- **bar-raise-2026-07-17-chains-compliance-02** (`blocked-on-user`, PP1, added 2026-07-17) — re-verified: `legal/terms.md` returns **zero** matches for refund / subscription / billing, while `src/subscription.ts` still renders the Cancel-anytime promise. **Unblocks when:** Kane names the refund policy and the billing terms — the drafting is fast, the policy is his call.
- **bar-raise-2026-07-17-chains-compliance-03** (`blocked-on-user`, PP1, added 2026-07-17) — re-verified: `_DRAFT_BANNER_HTML` is still defined at `src/legal-view.ts:295` and injected into both the Terms (`:316`) and the Privacy Policy (`:321`). Paying users are reading documents marked DRAFT. **Unblocks when:** Kane sets an effective date and says the Terms are final — removing the banner is a one-line edit once he does.
- **bar-raise-2026-07-16-yab-dependency-02** (`blocked-on-user`, PP1, added 2026-07-16) — re-verified both API calls live: `repos/TheProphetKane/yesandbudget/vulnerability-alerts` → **404 "Vulnerability alerts are disabled"**, `automated-security-fixes` → `{"enabled":false}`. The documentation half shipped in a962350; only the toggle remains. **Unblocks when:** Kane flips Dependabot alerts on in the repo's Settings → Security (no code change involved).
- **bar-raise-2026-07-16-yab-data-integrity-02** (`blocked-on-user`, PP1, added 2026-07-16) — re-verified by reproducing the failure against a fresh SQLite instance carrying the exact schema: the partial index at `db.ts:293` plus the bare conflict target at `sync.ts:102` raises `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`; adding `WHERE plaid_transaction_id IS NOT NULL` to the target prepares clean. **Unblocks when:** an attended Budget session lands the one-clause fix and ships it through `scripts/release.ps1`.
- **canonical-audit-2026-07-16-cattery-orders-cascade-prod-unapplied** (`blocked-on-user`, PP0, added 2026-07-16) — re-verified against live prod `emrerjdyujylgutfmool`: `orders_cattery_id_fkey` still reports `confdeltype='c'`. `supabase/migrations/0019_orders_cattery_fk_restrict.sql` exists on disk but `git ls-files` does not list it — it is untracked as well as unapplied. **Unblocks when:** Kane authorises DDL against live prod (`supabase db push`) plus committing the two untracked migrations.
- **bar-raise-2026-07-16-ring-verdict-enum-stale** (`blocked-on-user`, PP3, added 2026-07-16) — re-verified both halves: `X:\YesAndRing\scripts\write-dashboard-status.ps1:96` still pins `@('healthy','needs-attention','at-risk','stalled')`, and `status/data/Ring.json` currently carries `barRaise.verdict = "working"` — a value **not** in that list, so the next Ring status write nulls it. This one is live, not theoretical. **Unblocks when:** a Ring release runs (the one-line enum fix has no value until it ships, and this task is scoped no-publish).
- **bar-raise-2026-07-16-cattery-verdict-enum-stale** (`blocked-on-user`, PP3, added 2026-07-16) — re-verified: `X:\YesAndCattery\scripts\write-dashboard-status.ps1:83` carries the identical stale list. Cattery's current verdict is `needs-attention`, which happens to survive it, so the damage is latent rather than live. **Unblocks when:** a Cattery release runs — same shape as Ring, and both should ride the same pass.
- **bar-raise-2026-07-16-gnosis-secret-answers-ungated-api** (`blocked-on-user`, PP0, added 2026-07-16) — re-verified **live**, not by code read: `GET https://gnosis.yesandeverything.com/api/answers` returned **HTTP 200 with 380,188 bytes** to an unauthenticated caller. `web/worker.js:317` returns `loadAnswers(env)` with no gate check; `:320` does the same for `/api/answers/export`. Every other mutating route on the worker checks `env.GATE_PASSWORD` — these two reads do not. **Unblocks when:** Kane decides what the one public consumer of that endpoint should get instead (a filtered view, or its own key) — the worker change is small, the product call gates it.
- **gnosis-release-62-proposed-dm-answers-2026-07-16** (`blocked-on-user`, PP1, added 2026-07-16) — re-verified the strand: `git merge-base --is-ancestor 74ba52b HEAD` still exits 1, so the 4 residual answers are still only on the unmerged commit. `web/proposed-answers.json` currently holds 673 keys. **Unblocks when:** Kane gives an attended "go" for a Gnosis release (the recovery itself is scripted and scoped to 4 keys).
- **bar-raise-2026-07-10-yab-plaid-takeover-window** (`blocked-on-user`, PP1, added 2026-07-10) — re-verified: `apps/web/src/components/PlaidConnectFlow.tsx:90-94` still hardcodes a 90-day lookback with its own comment naming "Phase 2 should ask Plaid for the real `earliest_available_date`". Latent while Plaid is Sandbox-only. **Unblocks when:** Plaid moves out of Sandbox for a real account, or Kane decides 90 days is permanently good enough and the comment becomes a locked decision.
- **handler-audit-2026-07-10-gnosis-gate-password-decision** (`blocked-on-user`, PP1, added 2026-07-10) — re-verified all three sites: the literal `"DM Secrets"` still appears at `CLAUDE.md:33`, `PROJECT_SPEC.md:95` and `web/worker.js:8`; the repo remains private. **Unblocks when:** Kane picks a fork — accept the literal in a private repo and drop the `.secrets` framing, or scrub the files and rotate `GATE_PASSWORD` via wrangler.
- **handler-audit-2026-07-10-webhook-boilerplate** (`blocked-on-user`, PP2, added 2026-07-10) — re-verified by counting `X:\.secrets\<Project>\scripts\` on disk: Hordes 1, Chains 1, Scheduler 3, Apothecary 3, Budget 4, Rising 4, Ring 4, Gnosis 6 — against handlers that all promise five. Unchanged in the twelve days since the last reconciliation. **Unblocks when:** Kane decides whether five webhooks per project is the real standard (then provision the gaps) or aspirational (then the handlers say what actually exists).
- **scheduler-auditlog-composite-index-2026-07-10** (`blocked-on-user`, PP3, added 2026-07-10) — re-verified: `migrations/0011_audit_log_composite_index.sql` exists and is **untracked** in a tree that also carries 7 modified files. **Unblocks when:** Kane approves committing and applying Scheduler's parked migration — it is entangled with the stale-tree decision below, so one session settles both.
- **working-tree-2026-07-10-ring-stale-tree** (`blocked-on-user`, PP2, added 2026-07-10) — re-verified at Ring HEAD `389aadb`: still dirty, now **7** modified files (`CHANGELOG.md`, `CLAUDE.md`, `DEPLOY.md`, `PROJECT_SPEC.md`, `README.md`, `public/app.js`, `src/index.js`) — one more than last pass, so it is still accreting. **Unblocks when:** Kane reads the diffs and says commit-or-revert per file; two-week-old source may be a deliberate park, and guessing is the one thing this item forbids.
- **canonical-audit-2026-07-10-cattery-photos-bucket-listing** (`blocked-on-user`, PP1, added 2026-07-10) — re-verified against live prod `pg_policies`: the `photos public read` SELECT policy on `storage.objects` is **still present**, so bucket enumeration is still possible. `0018_drop_photos_public_listing.sql` is on disk and untracked. **Unblocks when:** Kane authorises the same prod DDL pass as the P0 above — both migrations should go together.
- **canonical-audit-2026-07-10-skylight-frame-id-empty-var** (`blocked-on-user`, PP2, added 2026-07-10) — re-verified: `wrangler.jsonc:41` still holds `"SKYLIGHT_FRAME_ID": ""` as a plain-text var while README and `src/index.ts` both instruct `wrangler secret put`. **Unblocks when:** Kane says whether the frame id is a secret or a var, and the loser is deleted from the other place.
- **canonical-audit-2026-07-10-gnosis-question-status-contract** (`blocked-on-user`, PP3, added 2026-07-10) — re-verified: `web/build.mjs:256` still defines `QSTATUS = { "?": "open", "x": "answered", "~": "lost" }` and `:391` still renders the "lost to time" chip, against a PROJECT_SPEC that says questions are never closed. **Unblocks when:** Kane rules on which is canon — the spec sentence or the two closed states — and the other is rewritten.
- **hbh-velocity-off-critical-path-mission-layer-2026-07-10** (`blocked-on-user`, PP1, added 2026-07-10) — re-verified at HBH v0.99.40: `source/data/mission.gd` and `source/data/missions/` still do not exist, and `source/autoloads/auto_pause.gd:88` still guards `has_signal("horde_day_announced")` against a `wave_director` that never declares it. **Unblocks when:** Kane commits R3D-04 as the next build target ahead of art-gated polish — this is a scope decision, not a coding one.
- **bar-raise-2026-07-10-skylight-\*** (11 items: `dependency-01/02`, `observability-01/02/03`, `maintainability-02`, `architecture-01`, `reliability-03`, `data-integrity-02/03/04`, `solo-tool-ux-01`) (`blocked-on-user`, PP2, added 2026-07-10) — **all re-verified individually this pass, all still exactly as filed:** zero `console.*` calls across all 12 `src/*.ts` files; `/api/health` still returns `ok:true` unconditionally at `src/index.ts:80-83`; `esc` still defined twice (`src/ui.ts:128` and `:393`); zero retry logic in `src/google.ts`; `Message-ID` read only at `src/email.ts:107` for `In-Reply-To` threading, never for dedupe; `epochOf` at `src/dates.ts:32-37` still anchors naive values to UTC while parsing absolute forms as instants (the docstring now *acknowledges* this rather than fixing it); `package.json:20` still pins `wrangler ^3.60.0`; `src/router.ts` still branches per-driver and hand-builds payloads. **Unblocks when:** Kane opens one attended Skylight session — these are eleven small changes with one gate, and the additive/low-risk rule means they can land together without touching the calendar write path.
- **bar-raise-2026-07-08-cattery-webhook-event-loss** (`blocked-on-user`, PP1, added 2026-07-08) — re-verified by reading the handler end to end: `src/index.js:320` marks the event seen via `srv_event_seen` **before** any DB write, every branch then falls through to `return json({ received: true })` regardless of whether the RPC succeeded, and `MAX_BODY_BYTES` at `:411` gates every route in `POST_ROUTES` including the webhook. A dropped write is invisible and unretryable. **Unblocks when:** an attended Cattery money-path session lands it — this touches order state, so it needs a real test against Stripe, not a code-read.
- **bar-raise-2026-07-08-cattery-money-path-logging** (`blocked-on-user`, PP1, added 2026-07-08) — re-verified with a count: `console.error` appears **zero** times in `src/index.js`. Every Stripe and RPC failure branch is silent in Workers logs. **Unblocks when:** same attended money-path session — it is the diagnostic that makes the webhook fix verifiable, so it should land first.
- **bar-raise-2026-07-08-cattery-stripe-idempotency-key** (`blocked-on-user`, PP2, added 2026-07-08) — re-verified: `Idempotency-Key` appears **nowhere** in the worker; the transfer POST at `src/index.js:302` sends none. **Unblocks when:** same attended money-path session (third of three; all touch the same file).
- **bar-raise-2026-07-08-cattery-ratelimit-key-bypass** (`blocked-on-user`, PP2, added 2026-07-08) — re-verified: `src/index.js:417` still keys the limiter on `auth.slice(-40)` — an unvalidated Bearer fragment — falling back to `cf-connecting-ip` only when the header is absent, so sending any garbage Bearer escapes the throttle. **Unblocks when:** an attended Cattery session flips the key order (IP first, validated user id second) and redeploys.
- **bar-raise-2026-07-07-ring-worker-observability** (`blocked-on-user`, PP1, added 2026-07-07) — re-verified: `wrangler.jsonc:11` still sets `"observability": { "enabled": true }` with no `logpush` and no `tail_consumer` anywhere in the file, so nothing is instrumented and nothing sinks. **Unblocks when:** an attended Ring session ships a release (all five Ring items below share this single gate).
- **bar-raise-2026-07-07-ring-community-ratings-backup** (`blocked-on-user`, PP1, added 2026-07-07) — re-verified: `handleJudgeRating` (`src/index.js:668`) still writes only `env.RATINGS.put(...)`; `supabase/migrations/` ends at `0007_user_state_rls.sql`, so no mirror migration was ever added. Months of cross-user voting data remain single-copy in KV. **Unblocks when:** an attended Ring session runs migration `0008` + `db push` + deploy.
- **bar-raise-2026-07-07-ring-dualwrite-atomicity** (`blocked-on-user`, PP1, added 2026-07-07) — re-verified: `src/index.js:808-809` still runs `env.RATINGS.put(...)` then `pgStateWrite(...)` as two bare sequential awaits with no marker and no repair-on-read. **Unblocks when:** the same attended Ring release pass.
- **bar-raise-2026-07-07-ring-worker-freetier-budget** (`blocked-on-user`, PP1, added 2026-07-07) — re-verified: the fan-out shape is unchanged in `src/index.js` (cron detail loop plus the typeahead handlers), with no adaptive backoff added. **Unblocks when:** Kane sets a subrequest budget he is willing to enforce — the throttle is easy, the ceiling is a product call.
- **bar-raise-2026-07-07-ring-gdpr-data-endpoints** (`blocked-on-user`, PP1, added 2026-07-07) — re-verified with a count: `/api/user/data` and `/api/user/export` match **zero** times in `src/index.js`. A paid product storing emails, cat names and private judge notes still has no export or deletion path. **Unblocks when:** the attended Ring release pass — this one also needs a privacy-page copy decision from Kane.
- **bar-raise-2026-07-07-ring-seed-pii-fictionalize** (`blocked-on-user`, PP2, added 2026-07-07) — re-verified in the served file: `public/data/seed.json` still carries "Pamela Ware" (L36-37), "Alexander Abbott" (L36), "Patricia Loynd" (L38) and `pam@topspotsfarm.com` (L82), shipped to every visitor of a live domain. **Unblocks when:** Kane confirms these names may be replaced (they are real people, so swapping them is a courtesy call as much as a code change) — then it is a mechanical fictionalize per the portfolio rule.
- **chains-br-auth-01-2026-07-04** (`blocked-on-user`, PP1, added 2026-07-04) — re-verified by reading the function: `_requireEntitledUser` (`worker/src/lib.js:1749`) still takes `body.userId` straight from the request, checks only that it is shaped like a UUID and not a `device:`/`dev_`/`ip:` prefix, then passes it to `_checkSubscriptionEntitled`. No Supabase JWT is verified anywhere in the path. Anyone who knows an entitled account's UUID gets Pro. **Unblocks when:** an attended Chains session lands JWT verification and ships it through `release.ps1` + worker deploy with a real end-to-end auth test.
- **bar-raise-2026-07-04-yab-bulk-delete-no-backup** (`blocked-on-user`, PP1, added 2026-07-04) — re-verified: `apps/api/src/server.ts:531-542` is still a bare `DELETE FROM transactions WHERE id IN (...)` with no snapshot and no undo. **Unblocks when:** an attended Budget session adds the pre-delete snapshot and ships it.
- **canonical-audit-2026-07-02-skylight-google-phase2-framing** (`blocked-on-user`, PP2, added 2026-07-02) — re-verified both sides: `.project-context.json:12` still labels the milestone "phase 1: web form + JSON API + email quick-add via the Skylight reverse API" and `:79` still calls Google "phase 2", while `src/router.ts:42` defaults `CALENDAR_DRIVER` to `google` and calls `google.createEvent`. The doc describes shipped, primary code as future work. **Unblocks when:** it rides the attended Skylight session with the eleven bar-raise items — it is the same file-and-context pass.
- **hbh-multimesh-crowd-migration-2026-06-28** (`blocked-on-user`, PP1, added 2026-06-28) — **scope narrowed on re-verify.** The finding said Crowd3D was complete but unwired; it is now wired — `source/world3d/enemy_field_3d.gd:88` reads `debug_flags.use_crowd3d`, `:446-454` loads and drives the crowd path, and `:451` falls back with a warning if the runner mesh is missing. What remains is that `source/autoloads/debug_flags.gd:33` still declares `use_crowd3d = false`, so the per-Node path is what ships. **Unblocks when:** someone runs the 2000/3000-enemy profile on the crowd path and Kane accepts the numbers — the migration work is done, only the default flip is outstanding.
- **yac-store-launch** (`blocked-on-user`, PP1, added 2026-06-10) — re-verified: Chains is at v0.58.3 with a `manifest.json` present but no `twa/` directory and no store listing work in tree. **Unblocks when:** Kane commits to a Play Store / TWA milestone — this is one of two gates between 92% and done, and it has never been started rather than stalled.
- **yac-subscriptions-enable-ai** (`blocked-on-user`, PP1, added 2026-06-10) — re-verified: `src/caddy.ts:62` still reads `export const CHAINS_VISIBLE = false`, and `src/subscription.ts` + `worker/src/handlers/billing.js` both exist, so the billing half is built and the flag is what holds. **Unblocks when:** Kane declares subscriptions production-ready and flips Decision 28 — note the two open Chains compliance items above are prerequisites, since flipping this exposes paying users to DRAFT terms.
- **hbh-asset-production-pass** (`blocked-on-user`, PP1, added 2026-06-10) — re-verified: HBH v0.99.40 still carries no `CREDITS.md` and no `LICENSE` file, and the art gap the gate names is unchanged. **Unblocks when:** Kane queues the Midjourney batches per the personalization standard — the inventory step (GDD Assets tab + `docs/ASSETS.md`) can happen unattended, the commissioning cannot.

### Hordes

34 aging items, all from the 2026-07-17 bar-raise wave (8 days). One older.

- **hbh-autopause-horde-day-signal-2026-07-14** (PP1, added 2026-07-14) — Do this session. Re-verified: `auto_pause.gd:88` still guards a signal `wave_director` never declares, and `connect_wave_director`/`connect_command_post` still have zero callers — the subsystem is dead end to end, and this is the whole-subsystem scope, not the signal slice.
- **bar-raise-2026-07-17-hordes-security-01** (PP1, added 2026-07-17) — Do this session. XSS sink at `GDD.html:6907`/`:8241`; inert only because the fetch 404s, which is a coincidence and not a mitigation.
- **bar-raise-2026-07-17-hordes-security-02** (PP2, added 2026-07-17) — Do this session. `cf-worker-canonical-state.js:89` skips the allowlist entirely on an absent Origin header — a public unauthenticated write; pairs with security-01 and the pending deploy.
- **bar-raise-2026-07-17-hordes-incident-runbook-currency-01** (PP1, added 2026-07-17) — Do this session. The v0.73.3 deploy never ran, so `/api/hbh-canonical-state` 404s; close the two security items above first, then deploy — one session covers all three.
- **bar-raise-2026-07-17-hordes-playability-02** (PP1, added 2026-07-17) — Do this session. `_toggle_pause` (`world_3d.gd:906`) flips a local bool while `enemy_field_3d._physics_process` keeps pathing and damaging the CP — pause is not authoritative, which is a player-visible bug.
- **bar-raise-2026-07-17-hordes-maintainability-01** (PP1, added 2026-07-17) — Defer to R3D-04. Reconciling the dead 778-line research autoload against the 5-tech stand-in is real, but which survives depends on the mission-layer decision that gates the escalated velocity item.
- **bar-raise-2026-07-17-hordes-reliability-02** (PP1, added 2026-07-17) — Defer to R3D-04. The four orphaned `economy.gd` mission-stat recorders have no call sites *because the mission layer does not exist*; wire-or-delete is premature until that decision lands.
- **bar-raise-2026-07-17-hordes-playability-01** (PP2, added 2026-07-17) — Defer to R3D-04. `[CHRONIC x7]` — this is the mission data layer itself, and it duplicates the escalated velocity item; it should be merged into it rather than carried twice.
- **bar-raise-2026-07-17-hordes-cost-economics-02** (PP1, added 2026-07-17) — Promote to scheduled task. 30-day spend tripled to $779.87 while commits fell 210→68; per-routine cost attribution is a standing measurement, not a one-off fix, and YaE's collector is where it belongs.
- **bar-raise-2026-07-17-hordes-skill-suite-health-01** (PP1, added 2026-07-17) — Do this session. Repackage and reinstall `personal-skills`; this is the same repackage that unblocks the escalated `handler-audit` skill item, so they close together.
- **bar-raise-2026-07-17-hordes-skill-suite-health-02** (PP1, added 2026-07-17) — Do this session. Repackage `code-audit` so the 2026-06-29 hazard catalog ships; rides the same repackage pass.
- **bar-raise-2026-07-17-hordes-skill-chain-handoff-integrity-01** (PP1, added 2026-07-17) — Do this session. The `(project, finding_id)` dedupe matched a *completed* item and silently swallowed HBH's only open HIGH for two runs — a queue-correctness bug that corrupts every future run until fixed.
- **bar-raise-2026-07-17-hordes-solo-dev-voice-portfolio-01** (PP1, added 2026-07-17) — Do this session. AI tool names in the public GDD changelog (Midjourney ×5, Claude ×4, a verbatim memory-key dump at v0.73.3) while `check_gdd_voice.py` passes green — public surface, and the gate that should catch it is blind.
- **bar-raise-2026-07-17-hordes-per-project-ps1-stack-01** (PP1, added 2026-07-17) — Do this session. Add the change-detected worker-deploy step to `release.ps1` (model on Chains `release.ps1:380-398`); this is the *cause* of the two-week-dead endpoint above, so it should land in the same pass.
- **bar-raise-2026-07-17-hordes-maintainability-04** (PP2, added 2026-07-17) — Do this session. Re-verified: `BEST_PRACTICES_PARITY.md:45` still claims world_3d.gd is "~800 lines" against a measured **957**, and unit_controller is **351** against a claimed 291. Small doc edit, numbers now confirmed.
- **hbh-gdd-settings-backbone-stale-2026-07-17** (PP2, added 2026-07-17) — Do this session. GDD §13.5 describes shipped settings as future work; release-gated (needs a pill bump) but trivial.
- **hbh-gdd-path-extends-phrasing-2026-07-17** (PP3, added 2026-07-17) — Do this session. One sentence at `GDD.html:4326` naming a retired convention; bundle with the item above into one release.
- **hbh-orphan-mainmenu-settings-deadcode-2026-07-17** (PP3, added 2026-07-17) — Do this session. Orphaned v0.x settings stub (`source/ui/main_menu/settings.gd`+`.tscn`+`.uid`), superseded by `source/ui/settings/`; delete after the documented uid grep.
- **bar-raise-2026-07-17-hordes-reliability-03** (PP2, added 2026-07-17) — Do this session. `_on_pause_menu`/`_on_pause_quit` discard the run with no save and no prompt while `options.gd:58` persists an `autosave_freq` that nothing reads — a data-loss path with a settings toggle that lies.
- **bar-raise-2026-07-17-hordes-data-integrity-01** (PP2, added 2026-07-17) — Do this session. Save/load erases the in-flight horde while restoring wave_director state, so a final-wave save fires a false `all_waves_cleared` on reload.
- **bar-raise-2026-07-17-hordes-save-format-stability-02** (PP2, added 2026-07-17) — Do this session. `user://options.tres` lacks the temp+readback+rename write `world3d_save.json` already has, and `global.gd:11-15` has no null fallback across 75 unguarded call sites.
- **bar-raise-2026-07-17-hordes-playability-03** (PP2, added 2026-07-17) — Do this session. Five of eight build hotkeys charge a different price than the menu shows; delete the `BUILDS` cost table and route through `BuildingCatalog3D`.
- **bar-raise-2026-07-17-hordes-scale-architecture-01** (PP2, added 2026-07-17) — Defer to the perf pass. `_ring_cells` is O(R³) per unit per frame; it belongs with the escalated MultiMesh default-flip decision, since both are measured on the same profile run.
- **bar-raise-2026-07-17-hordes-compliance-data-steward-01** (PP2, added 2026-07-17) — Do this session. Re-verified: neither `CREDITS.md` nor any `LICENSE` file exists, while the Godot logo (CC BY 4.0) is in the boot splash and Electrolize (OFL) is bundled. Attribution is an obligation, not a nicety.
- **bar-raise-2026-07-17-hordes-dependency-01** (PP2, added 2026-07-17) — Do this session. The vendored `addons/Tripo3d_Godot_Bridge/` is tracked and enabled at `project.godot:43` with no license and no provenance row, falsifying `ASSET_PROVENANCE.md`; register it or drop it.
- **bar-raise-2026-07-17-hordes-asset-pipeline-02** (PP2, added 2026-07-17) — Do this session. `art-check.yml` filters only `assets/**` and `check_art.py` has no `.glb` branch, so 51 of 55 tracked `.glb` skip the documented silent-placeholder trap.
- **bar-raise-2026-07-17-hordes-incident-runbook-currency-02** (PP2, added 2026-07-17) — Do this session. `cf-worker-setup.md` followed literally today orphans the live KV namespace holding real canonical state — a runbook that destroys data is worse than none.
- **bar-raise-2026-07-17-hordes-audit-loop-integrity-02** (PP2, added 2026-07-17) — Promote to scheduled task. `write-dashboard-status.ps1:113/173` re-reads its own prior value, making 0/0/0 a permanent fixed point; the same defect is filed independently against Rising (`observability-02`), so it is a portfolio-wide writer bug.
- **bar-raise-2026-07-17-hordes-scheduled-task-firing-state-01** (PP2, added 2026-07-17) — Do this session. Add the `write-dashboard-status.ps1` step to `audit-hordes-nightly`, mirroring `audit-apothecary-nightly:15`.
- **bar-raise-2026-07-17-hordes-audit-loop-integrity-01** (PP2, added 2026-07-17) — Do this session. Record that HBH's canonical doc is release-gated, so the empty auto-fix lane reads as structural rather than as a failure every run.
- **bar-raise-2026-07-17-hordes-work-queue-hygiene-01** (PP2, added 2026-07-17) — Drop candidate next pass, keep now. It describes HBH's queue slice as 9 items with 3 blocked P1s; the slice is now 34 aging + 3 escalated, so the specific counts are stale — but the underlying claim (nothing drains automatically) is *more* true, not less. Re-file with current numbers rather than delete.
- **bar-raise-2026-07-17-hordes-skill-trigger-collisions-01** (PP2, added 2026-07-17) — Do this session. `[CHRONIC x7]` — correct routing depends on the `anthropic-skills` copy shipping no description; replace the trigger phrase with a fully-qualified id. Rides the repackage pass.
- **bar-raise-2026-07-17-hordes-voice-and-tone-01** (PP2, added 2026-07-17) — Defer to the next naming pass. Six research nodes off the Order 42 frame; real, but cosmetic and cheaper batched with other content work.
- **bar-raise-2026-07-17-hordes-version-pill-drift-01** — **DROPPED**, see Auto-applied.

### Everything

28 aging items, all from the 2026-07-17 bar-raise (8 days). This is the portfolio's own infrastructure, so its defects multiply across every other project.

- **bar-raise-2026-07-17-yae-yae-tracked-queue-artifacts-public** (PP1, added 2026-07-17) — Do this session. 68 tracked paths matched by ignore rules added *after* tracking (so they never bound) plus 4 matching no rule at all, all serving 200. `git rm --cached` alone re-stages them under `git add -A`; needs the ignore fix in the same commit.
- **bar-raise-2026-07-17-yae-yae-release-add-all-sweeps-operational-state** (PP1, added 2026-07-17) — Do this session. `scripts/push-to-github.ps1:92` `git add -A` sweeps 15 machine-churned files including a mid-write 592KB queue — and it is the mechanism behind the item above. Fix them together.
- **bar-raise-2026-07-17-yae-yae-gitignore-no-secret-shape-patterns** (PP1→ treat as P1, added 2026-07-17) — Do this session. Zero env/token/secret/webhook/pat/credential patterns across all 56 `.gitignore` lines while `git add -A` stages everything; third leg of the same tripod.
- **bar-raise-2026-07-17-yae-yae-cp1252-json-readers-mojibake-live** (PP1, added 2026-07-17) — Do this session. Twelve `Get-Content` sites lack `-Encoding`, and the corruption is live in `status/data/Ring.json` and the dashboard right now. I hit this same class of bug reading the queue at the top of this run.
- **bar-raise-2026-07-17-yae-yae-release-step-ordering-corrupt-json-ships** (PP1, added 2026-07-17) — Do this session. `release.ps1` validates status JSON at Step 1 *before* Step 2 writes it, and `write-dashboard-status.ps1:55` Move-Items over the live file before every post-write throw, so corrupt JSON ships with only a WARN.
- **bar-raise-2026-07-17-yae-yae-guards-absent-from-workflow-path** (PP1, added 2026-07-17) — Do this session. Every integrity guard sits on `release.ps1`, which carries almost no traffic: 25 of the last 25 commits reached main through writers that cross no guard. Pairs with the item above — the ordering fix is worthless if nothing runs it.
- **bar-raise-2026-07-17-yae-yae-skipgate-cannot-fire** (PP1, added 2026-07-17) — Do this session. The skip-gate cannot fire for three independent reasons and the previously-queued fix addresses one, so it would buy nothing; re-scope before spending on it.
- **bar-raise-2026-07-17-yae-yae-barraise-run-cost-unmeasured** (PP1, added 2026-07-17) — Promote to scheduled task. Measured floors of $35.59 and $44.54 in lens subagents alone against `actionsClosed 1` across nine runs. Cost-per-closed-action is the number that decides bar-raise cadence portfolio-wide, and it needs to be tracked continuously, not audited once. Merges naturally with the Hordes `cost-economics-02` item.
- **bar-raise-2026-07-17-yae-yae-local-git-never-packed** (PP2, added 2026-07-17) — Do this session. Re-verified and **worse**: `git count-objects -v` now reports **24,351** loose objects (was 23,234), `in-pack 0`, `packs 0`, **1.21 GiB** against a 58MB working tree, plus 16 `tmp_obj_*` garbage files from interrupted FUSE writes. A `git gc` is one command and this grows every day it waits.
- **bar-raise-2026-07-17-yae-yae-queue-no-enum-validation** (PP2, added 2026-07-17) — Do this session. Confirmed independently this run: I found `priority` as both `"P1"` and bare `2`, `project` spelled `Ring`/`ring` and `Cattery`/`cattery` and `Gnosis`/`gnosis` — every consumer of this file has to normalize by hand, and `collect-usage.ps1:817` silently misattributes what it cannot match.
- **bar-raise-2026-07-17-yae-yae-statusjson-contract-unversioned** (PP2, added 2026-07-17) — Do this session. Re-verified: **0 of 13** files in `status/data/` carry a `schemaVersion`, and Skylight.json forks on both identity keys with nothing detecting it.
- **bar-raise-2026-07-17-yae-yae-four-project-registries** (PP2, added 2026-07-17) — Do this session. Four hand-maintained project lists in three languages with no assertion binding them; add the assertion, since the drift is silent by construction.
- **bar-raise-2026-07-17-yae-yae-archive-not-clean-terminal-store** (PP2, added 2026-07-17) — Do this session. 4 duplicate ids in the archive and one id `pending` on *both* sides, while `queue-edit.ps1:158-162` drop deletes outright rather than archiving.
- **bar-raise-2026-07-17-yae-yae-write-dashboard-status-no-retry** (PP2, added 2026-07-17) — Do this session. `write-dashboard-status.ps1:50-58` is single-shot where its sibling `queue-edit.ps1:100-110` retries 5× with a settle — same protocol, one hardened, one not.
- **bar-raise-2026-07-17-yae-yae-stamper-silent-stale** (PP2, added 2026-07-17) — Do this session. `update-project-pages.mjs` skips malformed status JSON with a `console.error` and always exits 0, so a broken stamp is indistinguishable from a clean run.
- **bar-raise-2026-07-17-yae-yae-queue-get-returns-unfiltered-blob** (PP2, added 2026-07-17) — Do this session. `queue-edit.ps1` has no list/filter/summary op and `get` emits the entire 692KB queue. Every routine that reads the queue pays this; I worked around it with ad-hoc Python this run rather than use the tool.
- **bar-raise-2026-07-17-yae-yae-work-queue-runner-pwsh-not-installed** (PP2, added 2026-07-17) — Do this session. Re-verified: `pwsh` is still not on PATH (host is Windows PowerShell 5.1) while `work-queue-runner/SKILL.md:26,28,29,31` all lead with it. Four-line find-and-replace.
- **bar-raise-2026-07-17-yae-yae-project-context-stale-and-v1** (PP2, added 2026-07-17) — Do this session. `.project-context.json` is on schema v1 while 12 of 13 projects are v1.1, with a null `release_script` against a real `scripts/release.ps1` and a `queue-drain-4h` task that does not exist.
- **bar-raise-2026-07-17-yae-yae-skillreview-tree-public** (PP2, added 2026-07-17) — Do this session. 143 tracked files under `_skill-review/` serve 200; `robots.txt:12` disallows the path, which stops crawlers and nobody else. Same family as the tracked-artifacts item — decide gate-or-untrack once for both.
- **bar-raise-2026-07-17-yae-yae-skill-artifacts-stale-vs-source** (PP2, added 2026-07-17) — Do this session. 10 of 19 packaged `.skill` artifacts predate their source (one by 35 days) and no pack script exists. This is the *same root cause* as the escalated `handler-audit` skill item, where I confirmed the source is fixed and the installed copy is not. Write the pack script and it closes three items at once.
- **bar-raise-2026-07-17-yae-yae-amber-token-means-three-colors** (PP2, added 2026-07-17) — Do this session. Re-verified: `--amber` is `#cc8400` (`index.html:63`), `#cc5500` (`projects/budget/index.html:39`) and `#f5b860` (`status/index.html:28`).
- **bar-raise-2026-07-17-yae-yae-subpage-no-reduced-motion-focus-visible** (PP2, added 2026-07-17) — Do this session. `:focus-visible` appears 7× in `index.html` and **zero** times across all 18 other pages; keyboard users lose the focus ring everywhere but the homepage.
- **bar-raise-2026-07-17-yae-yae-subpage-no-heading-structure** (PP2, added 2026-07-17) — Do this session. Seven project pages are h1=1/h2=0/h3=0 and `404.html` has no h1 at all; bundle with the accessibility item above into one sweep.
- **bar-raise-2026-07-17-yae-yae-budget-scheduler-off-template** (PP2, added 2026-07-17) — Do this session. The last two pages on a superseded template generation (no og tags, no canonical, unlinked crumb). Overlaps the escalated Scheduler gate-page prose item — do the template migration and the rewrite together.
- **bar-raise-2026-07-17-yae-yae-gate-payload-decode-guard-residual** (PP2, added 2026-07-17) — Do this session. `push-to-github.ps1:53-61` tests only `Test-Path` and a regex for the encoded assignment, decodes no base64, and has no brackish-rising equivalent at all. This is the guard that should have caught the escalated Hordes gate-page leak.
- **bar-raise-2026-07-17-yae-yae-handover-roadmap-stale-scheduling** (PP2, added 2026-07-17) — Do this session. `BAR_RAISE_ROADMAP.md:3` says "Active" while its own table at `:301-312` marks all seven phases DONE, and HANDOVER references a `scripts/schedule/` absent from disk and git.
- **bar-raise-2026-07-17-yae-yae-housekeeping-tracked-cruft** (PP2, added 2026-07-17) — Do this session. 56 untracked loop probes, 5 tracked queue snapshots (~360KB) needing `git rm`, 4 tracked dated reports predating the retention rules, 7 unreferenced scripts. Squarely the "no rotting tracked items" rule.
- **bar-raise-2026-07-17-yae-yae-staged-fixes-unshipped-since-0710** — **DROPPED**, see Auto-applied.

### Rising

19 aging items, all from 2026-07-17 (8 days).

- **bar-raise-2026-07-17-rising-solo-tool-ux-01** (PP2, added 2026-07-17) — Do this session. Re-verified: **zero** `2>&1`/`Invoke-Git` occurrences in either `scripts/push-to-github.ps1` or `scripts/publish-gdd.ps1`, while `write-dashboard-status.ps1:237` already carries the guard. This is the root of the recurring release abort, and Budget already shipped the shared-helper fix (`Invoke-Git`, YaB v0.14.10) that can be lifted straight across.
- **bar-raise-2026-07-17-rising-architecture-01** (PP2, added 2026-07-17) — Do this session. Re-verified `scripts/check_best_practices.py:23-30`: still a hardcoded six-name `GOD_FILES` list, all six now passing, while `building.gd`, `wave_director.gd`, `settings.gd` and `enemy_pool.gd` are over 1000 lines and invisible to the ratchet. A green gate that cannot see the violations is worse than a red one.
- **bar-raise-2026-07-17-rising-orchestration-03** (PP2, added 2026-07-17) — Do this session. Re-verified: `.project-context.json:63` points `hazard_catalog` at `personal-skills-src/skills/code-audit/hazards/per-project/BR.md`, which does not exist on disk. Repoint or remove.
- **bar-raise-2026-07-17-rising-compliance-data-steward-01** (PP2, added 2026-07-17) — Do this session. Re-verified: no `docs/LICENSES.md` exists, while ~5.4 MB of spine-godot DLLs and Navy's commissioned art sit in tree with no recorded license tier or redistribution terms. Record-what-you-have work, and the commissioned-art terms are the part that gets expensive to reconstruct later.
- **bar-raise-2026-07-17-rising-maintainability-03** (PP2, added 2026-07-17) — Do this session, **narrowed.** The docs half is done — `docs/ARCHITECTURE.md:21` now correctly tombstones `ASSET_SCALE_MASTER` as RETIRED. The code half is not: `scripts/check_playability.py:43` still reads `cfloat("ASSET_SCALE_MASTER", 1.0)` and `:13`/`:63` still reference it in the failure message, against the locked no-scale-constants decision.
- **bar-raise-2026-07-17-rising-maintainability-01** (PP2, added 2026-07-17) — Do this session. `GODOT_BEST_PRACTICES_ADHERENCE.md` was touched 2026-07-24 and has grown real v0.59.42/43 entries, but §1/§10 still frame the god-files in pre-decomposition present tense (`:52` still says "the three god-files"). Small rewrite, not a delete.
- **bar-raise-2026-07-17-rising-maintainability-02** (PP2, added 2026-07-17) — Do this session. `docs/ARCHITECTURE.md` now carries wave-2 material, so this is narrower than filed: what remains is the stale line counts and the Scout base contradiction.
- **bar-raise-2026-07-17-rising-data-integrity-02** (PP2, added 2026-07-17) — Do this session. No save round-trip coverage in `tests/run_tests.gd`; `save_load.gd` is exercised only by hand. Pairs with the two save-validation items below — one test session covers all three.
- **bar-raise-2026-07-17-rising-data-integrity-03** (PP2, added 2026-07-17) — Do this session. `save_load.gd` restores tiles and instantiates entities without checking `iso_grid.is_in_bounds` first.
- **bar-raise-2026-07-17-rising-game-design-02** (PP2, added 2026-07-17) — Do this session. `save_load.gd:388-398` detects a grid-size mismatch and only `push_warning()`s, then restores anyway — out-of-bounds entities land in the world. Same file, same session as the two above.
- **bar-raise-2026-07-17-rising-reliability-02** (PP2, added 2026-07-17) — Do this session. `slide_move`, `apply_safe_push` and `push_edge_ok` carry the v0.59.22-24 collision contract and the v0.59.23 LOS corner fix with **no test guarding any of it** — and memory records that corner-cut bug as a root cause once already.
- **bar-raise-2026-07-17-rising-performance-04** (PP2, added 2026-07-17) — Do this session. `enemy_base.gd:794-815` walks the full units and buildings group arrays per enemy per acquisition while `spatial_index.gd` already provides the bucketed lookups; the index was extracted in v0.59.9 specifically for this.
- **bar-raise-2026-07-17-rising-performance-01** (PP2, added 2026-07-17) — Defer to the next art drop. A peak-wave frame profile is the right baseline, but capturing it before Navy's remaining art lands means re-capturing after.
- **bar-raise-2026-07-17-rising-observability-02** (PP2, added 2026-07-17) — Promote to scheduled task. `write-dashboard-status.ps1:165-172` never parses the audit report, so Rising.json renders 0/0/0 regardless — **the same defect filed independently against Hordes** (`audit-loop-integrity-02`). Fix it once in the shared writer shape rather than per project.
- **bar-raise-2026-07-17-rising-compliance-data-steward-03** (PP2, added 2026-07-17) — Do this session. The `no_hbh_cross_contamination` check greps the whole tree and false-positives on `docs/GDD.html`, where the fork history legitimately names the HBH lore. A gate that always fails never blocks.
- **bar-raise-2026-07-17-rising-public-voice-01** (PP2, added 2026-07-17) — Do this session, **in YaE not BR.** Two em-dash entities on the YaE-side gate page; the item itself flags the scope crossing, and the standing scope rule means it must be worked from a YaE session.
- **bar-raise-2026-07-17-rising-game-design-03** (PP2, added 2026-07-17) — Do this session. `unit.gd:37-38` describes a superseded scaling model; the inherited HBH version stamps are the known-benign v0.60-0.65 population (memory `br-inherited-hbh-version-stamps`), so touch only the ones this item names — no mass rewrite.
- **bar-raise-2026-07-17-rising-strategic-kill-this-01** — **DROPPED**, see Auto-applied.
- **bar-raise-2026-07-17-rising-solo-tool-ux-04** — **DROPPED**, see Auto-applied.

### Gnosis

9 aging items (8-9 days).

- **canonical-audit-2026-07-16-gnosis-public-to-gated-links** (P2, added 2026-07-16) — Do this session. Player-facing pages linking into gated surfaces; small and squarely inside the standing question-board split rule.
- **canonical-audit-2026-07-16-gnosis-catalog-ogl-provenance** (P3, added 2026-07-16) — Do this session. OGL provenance on the catalog; the SRD open5e-link rule already gives the shape, this is applying it.
- **bar-raise-2026-07-16-gnosis-release-gate-check-page-only** (PP1, added 2026-07-16) — Do this session, **first.** This is the *reason* the escalated P0 shipped past nine releases: the deploy check asserts 401 on a page while the data path is ungated. Fixing the check is what stops the next one.
- **bar-raise-2026-07-16-gnosis-uncapped-unauth-kv-writes** (PP2, added 2026-07-16) — Do this session. Unauthenticated KV writes with no cap on a live public Worker; same file and same session as the gate work above.
- **bar-raise-2026-07-16-gnosis-kv-write-failures-swallowed** (PP2, added 2026-07-16) — Do this session. Silent KV write failures — the diagnostic that makes the two items above verifiable.
- **bar-raise-2026-07-16-gnosis-answers-full-store-walk** (PP2, added 2026-07-16) — Do this session. A full-store walk per request; directly relevant to the P0, since the ungated endpoint I measured returned 380KB in one call.
- **bar-raise-2026-07-16-gnosis-buildmjs-seven-esc-defs** (PP2, added 2026-07-16) — Do this session. Filed against seven `esc` definitions; last pass measured nine in a 6,345-line file, so it grows every week it waits.
- **canonical-audit-2026-07-17-gnosis-character-gate-undocumented** (P1, added 2026-07-17) — Do this session. An undocumented gate on a live DM-gated site is exactly the class of thing the P0 came from.
- **canonical-audit-2026-07-17-gnosis-milestone-stamp-hand-maintained** (P3, added 2026-07-17) — Promote to scheduled task. A hand-maintained stamp will drift again; derive it in the release script.

### Budget

5 aging items (8-9 days). All three finance-product items are money-correctness.

- **bar-raise-2026-07-16-yab-finance-product-categorization-tail-01** (PP1, added 2026-07-16) — Do this session. `[CHRONIC runsOpen 7]` — per-order tokens in merchant descriptors normalize to a unique string per transaction, so the categorization tail can never shrink for those merchants.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-02** (PP1, added 2026-07-16) — Do this session. The natural key at `db.ts:120` has no occurrence ordinal, so two legitimately distinct same-day same-amount charges collide. Note this shares a file with the escalated `data-integrity-02` I reproduced above — sequence them in one session, dedupe first.
- **bar-raise-2026-07-16-yab-finance-product-dedupe-04** (PP1, added 2026-07-16) — Defer to dedupe-02. Explicitly downstream: "once data-integrity-02 is fixed, the pending-to-posted transition collides on the same key". Working it first would be wasted.
- **bar-raise-2026-07-17-yab-finance-product-categorization-tail-02** (PP1, added 2026-07-17) — Do this session. The Categorize page saves a rule whose pattern is the entire normalized merchant string as an exact substring; the correct pattern already exists at `Transactions.tsx:275-291`, so this is lifting a known-good shape one layer down.
- **bar-raise-2026-07-16-cross-budget-openfindings-backfill** (PP2, added 2026-07-16) — Do this session. Parts (1) and (2) shipped; only the historical backfill remains, and it is mechanical.

### Chains

4 aging items (8 days). Note these sit behind the two escalated compliance items — Chains takes real money today.

- **bar-raise-2026-07-17-chains-dataint-01** (PP1, added 2026-07-17) — Do this session. `worker/src/handlers/billing.js:640-693` has no reconciliation and no `charge.refunded`/dispute branch, so one dropped webhook strands a paying user without Pro or leaves a refunded user entitled. Money-correctness on a live paid product.
- **bar-raise-2026-07-17-chains-finance-02** (PP2, added 2026-07-17) — Do this session. No dunning decision anywhere: neither `lib.js:1691` nor `is_entitled()` has a `past_due` branch, so a failed renewal is undefined behaviour. Kane should name the grace period; the code follows.
- **bar-raise-2026-07-17-chains-cost-01** (PP2, added 2026-07-17) — Do this session. `handleExtractPhotosReal` and `handleParseCourseText` never apply `DAILY_AI_SPEND_CAP_USD` and `photos[]` is unbounded — two AI endpoints that bypass the global spend cap on a worker that pays real Anthropic costs.
- **bar-raise-2026-07-17-chains-dataint-02** (PP2, added 2026-07-17) — Do this session. The spend counters are a KV read-modify-write, so concurrent requests interleave and the caps overshoot. Same session as cost-01 — one is the missing gate, the other is the leaky one.

### Skylight

3 aging items (8-9 days), on top of 15 escalated. Additive/low-risk only, per the standing rule.

- **canonical-audit-2026-07-02-skylight-changelog-phantom** (PP3, added 2026-07-16) — Do this session. A changelog entry for something that never shipped; one-line delete.
- **bar-raise-2026-07-17-skylight-maintainability-01** (PP1, added 2026-07-17) — Do this session. Re-verified: **no test files and no vitest config anywhere** in the repo, while `src/dates.ts` and `src/parse.ts` are pure, fragile and untested — and a timezone bug shipped *and was fixed* in exactly that code inside one month. Adding tests is additive by definition, so it clears the low-risk bar cleanly.
- **bar-raise-2026-07-17-skylight-security-01** (PP2, added 2026-07-17) — Do this session. Re-verified at `src/email.ts:47`: `if (allowed.length && !allowed.includes(fromAddr))` — an unset or empty `ALLOWED_SENDERS` skips the allowlist entirely, so the inbound-mail gate fails **open** onto a family calendar. One-word fix (`if (!allowed.length || ...)`), and it is the highest-consequence item in Skylight's whole slice.

### Apothecary

3 aging items (8 days).

- **working-tree-2026-07-17-apothecary-stale-tree** (PP2, added 2026-07-17) — Do this session, **narrowed.** Re-verified: `DEPLOY.md` — the 5-weeks-unshipped file the item was actually about — is now **clean and committed.** The tree is still dirty, but with `PROJECT_SPEC.md` and `src/main.js`, neither of them stale. Re-file against the current pair rather than drop, since the tree never actually got clean.
- **bar-raise-2026-07-17-apothecary-discord-notify-header-stale** (PP1, added 2026-07-17) — Do this session. `scripts/discord-notify.ps1`'s own header names a path its code does not read; a comment that lies about the file beside it.
- **bar-raise-2026-07-17-apothecary-comment-accuracy-sweep** (PP3, added 2026-07-17) — Do this session. `src/main.js:75` dates the migrate.js extraction to a v1.0.4 that does not exist; bundle with the item above into one release.

### Ring

2 aging items, on top of 8 escalated.

- **canonical-audit-2026-07-12-ring-discord-role-mismatch** (PP3, added 2026-07-12) — Do this session. **Oldest item in the aging set at 13 days.** A webhook role-name mismatch against the portfolio `<id> <Role> Bot` standard; trivially small, and it has aged purely because nothing small ever gets picked up.
- **canonical-audit-2026-07-17-ring-scrubdash-emdash-guard** (PP2, added 2026-07-17) — Do this session. Em-dash guard on the scrub path; same family as the Rising public-voice item, and both are one-line gate extensions.

### Cattery

1 aging item, on top of 8 escalated.

- **working-tree-2026-07-17-cattery-stale-docs-tree** (PP2, added 2026-07-17) — Do this session. Re-verified and **grown**: the tree now carries `PROJECT_SPEC.md`, `docs/DATA-MODEL.md`, `test/money.test.js` modified plus **four untracked migrations** (`0009`, `0010`, `0018`, `0019`). Two of those untracked files are the security fixes the P0 and the photos-bucket item are blocked on, so this is no longer doc hygiene — it is the same blocker wearing different clothes.

### Cross-project

1 aging item.

- **working-tree-2026-07-17-queue-duplicate-stale-tree-items** (PP2, added 2026-07-17) — Do this session, **narrowed.** Part (1) is done: three of the four duplicate ids no longer exist in the queue and the survivor is escalated, not pending. Parts (2) and (3) are not — re-verified that **Ring is still dirty (7 files)** and **Scheduler is still dirty (7 modified + `migrations/0011` and `scripts/sync-audit-dashboard.ps1` untracked)**, both unchanged in shape since filing. The item's own instruction stands: ask Kane before committing either project's source diffs, because two-week-old uncommitted source may be a deliberate park.

### Auto-applied this pass

Four items removed from `.work-queue.json` after verifying their evidence is now false.

- **bar-raise-2026-07-17-yae-yae-staged-fixes-unshipped-since-0710** (P1, everything) — the finding was that `CLAUDE.md`, `scripts/update-project-pages.mjs` and `sitemap.xml` had sat uncommitted since 2026-07-10. All three now show a `git log -1` date of **2026-07-24**, and `git status --porcelain` on all three returns empty. The unshipped diff shipped.
- **bar-raise-2026-07-17-rising-strategic-kill-this-01** (P1, rising) — the finding was that `.project-context.json` read `pct 50, anchored 2026-06-23, remaining[] empty`. It now reads `pct 50, anchored 2026-07-18` with **four populated `remaining[]` entries**, the last of which explicitly states the basis for holding at 50 ("evidence-anchored here, not moved, and will only advance when a real gameplay deliverable lands"). That is precisely what the item asked for.
- **bar-raise-2026-07-17-hordes-version-pill-drift-01** (P2, hordes) — the finding was that the YaE live markers read v0.99.38 against a repo at v0.99.40. `projects/here-be-hordes/index.html:85` and `design.html:102` **both now read v0.99.40**, matching `project.godot` `config/version="0.99.40"`. Markers are in sync.
- **bar-raise-2026-07-17-rising-solo-tool-ux-04** (P2, rising) — the finding was that `X:\BrackishRising\CLAUDE.md` named the release.ps1 stderr-abort but "records no recovery procedure". `CLAUDE.md:18` now states both the symptom and the recovery verbatim: "Finish the commit / push / publish steps via the Bash tool (POSIX git) when that happens." The procedure is recorded. (The separate *cause* fix — adding the `2>&1` guard to the two bare-git scripts — remains queued as `rising-solo-tool-ux-01` and is untouched by this drop.)

No escalated item was reset to `pending` or marked `completed` this pass: all 54 were re-verified and all 54 still hold.
