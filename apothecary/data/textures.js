// textures.js - auto-generated by scripts/regen-textures-manifest.ps1
//
// Lists every parchment-* file in data/textures/, regardless of extension.
// Files are pre-sorted lightest -> darkest by sort-textures-by-luminance.ps1.
// Slots 9-16 (the darker half) carry a per-slot opacity so they read as
// label backgrounds rather than overwhelming the text. Slot 1 (lightest)
// is the default; the v0.8.1 gradient option has been retired.
//
// Don't hand-edit. Re-run sort + regen after adding or removing textures.

export const PARCHMENT_TEXTURES = [
  { id: 'parchment-01', label: 'Parchment 01',                file: 'parchment-01.png', opacity: 1.00 },
];

// Default: the lightest texture (parchment-01) after the sort pass.
export const DEFAULT_PARCHMENT = PARCHMENT_TEXTURES.length > 0 ? PARCHMENT_TEXTURES[0].id : null;
